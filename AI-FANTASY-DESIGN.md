# Fantasy 单人 AI 对战开发文档

## 1. 交付物与目标

文件：`F:\fantasy\pokemon-showdown\AI-FANTASY-DESIGN.md`，使用中文 Markdown，随服务端仓库管理。

用户已于 2026-09-07 明确要求按照本文继续完成开发。本文保留已确认的需求和验收标准，实现状态持续记入进度文档。

截至 2026-09-07，服务端已实现 AI 基础、策略与有限推演、挑战命令和对局生命周期；正常客户端已接入首页入口、训练家与队伍选择、明确的创建结果及重赛返回。普通档已完成本机网页的多回合、重连、认输和回放下载验证；完整网页异常矩阵与强度验收仍待完成。具体实现及验证范围见 [AI-FANTASY-PROGRESS.md](AI-FANTASY-PROGRESS.md)，本机启动见 [AI-FANTASY-LOCAL.md](AI-FANTASY-LOCAL.md)。下文的需求与验收指标不能视为已全部交付。

目标是提供接近真人决策方式的固定训练家挑战：具备对局记忆、有限预判、战术风格和资源管理。首版不采用强化学习、大模型决策或 Showdex。

提供普通、高难两档。两档共用策略、计算预算和训练家队伍，差异仅为可获得的信息。

## 2. 玩法与信息边界

### 训练家挑战

- 玩家在对战前自由选择 FC UBUU、FC OU、FC UU，再选择该赛制下的训练家、难度和自己的队伍。
- 每名训练家固定使用一套队伍，由用户提供或按用户明确要求编写；文档定义配置和接入方式。
- 每个训练家配置项指定一种现有 FC 赛制，双方队伍必须通过对应合法性检查。FC UBUU 对应现有的 FC Ubers UU；同一角色在不同赛制使用不同标识的配置项与各自固定队伍。
- 首版限定常规六对六、每只最多四个招式的单打；不包含九选六、双打、多人、自定义超大队伍或超多招式。
- 对战不计入真人天梯。结束后支持重新挑战和现有回放功能。
- 首版不做连续闯关、解锁、奖励和账号挑战战绩。

### 两档信息权限

| 信息 | 普通档 | 高难档 |
|---|---|---|
| AI 自己的队伍及当前状态 | 完整可见 | 完整可见 |
| 玩家公开的队伍预览、对战日志 | 可见 | 可见 |
| 玩家全队初始招式 | 开局额外获得 | 开局额外获得 |
| 玩家全队初始种族、形态、等级、道具、特性、太晶属性 | 仅公开或推测部分 | 开局额外获得 |
| 玩家全队初始精确六项能力值 | 只能推测 | 开局额外获得 |
| 玩家当前精确血量、剩余 PP、隐藏状态 | 不直接读取 | 不直接读取 |
| 玩家本回合已合法提交但尚未执行的招式 | 不可见 | 可见；换人不读取目标 |
| 战斗随机数状态及未来随机结果 | 不可见 | 不可见 |

普通档新增全队配招快照；高难档保留**初始完整配置快照**，并在玩家合法提交后读取本回合所选招式。后续的道具交换、特性变化、形态变化等按公开信息更新。2026-09-10 的最新信息权限、撤销/改选处理及验证范围见 [AI-FANTASY-INFORMATION.md](AI-FANTASY-INFORMATION.md)。

两档都可以根据线索推理幻觉，但不得通过内部对象或队伍槽位直接识破。已知玩家全队初始配置，也不代表知道队伍预览阶段的排序选择。

客户端明确说明高难档的信息优势。额外快照只在服务端内部使用，不广播给观战者或写入公开回放。

## 3. 策略与技术设计

### 决策流程

采用统一流程：

**允许读取的信息 → 对局记忆与配置推测 → 合法行动枚举 → 规则评分 → 逐步加深的有限推演 → 选择行动。**

- 记录已公开招式、道具、特性、速度关系、近期换人和特殊资源使用情况。
- 普通档使用已知全队配招，依据现有 Fantasy 数据和配置样例推测其他配置，不依赖在线数据服务。
- 高难档用初始快照替换相应配置假设，并针对本回合已提交招式应对；未获授权的动态信息仍需推测。
- 规则评分覆盖击杀、命中风险、先制、恢复、强化、场地、异常状态、换人和关键成员保留。
- 枚举赛制允许的 Mega、Mega X/Y、Z 招式、太晶化和气场爆发等操作，检查资源消耗与互斥关系。
- 根据近期行为抑制无收益的反复换人；只在评分接近的合理行动之间保留随机选择。
- 队伍预览和强制换人使用专门评分流程，不能机械套用普通攻击回合。

### 战术风格

训练家可配置均衡、强攻、稳健三种初始风格，以及关键成员和特殊资源偏好。

风格调整收益与风险权重，不强制执行固定回合脚本。同一训练家的普通、高难档使用相同风格。

### 有限推演

- 使用当前 Fantasy 模拟引擎，先完成一回合比较，再在预算内逐步加深：常规最多四回合，极少量关键决策最多六回合。默认时间上限分别为 10 秒和 20 秒，不设固定累计模拟回合数截断。
- 常规我方六个根候选、深层三个；关键八个根候选、深层四个。对方四个候选、四种配置或状态假设、每组合两个随机样本；近截止时不启动预计无法完成的新一轮。
- 急速折返、倒下补位及后续回合由规则策略根据新局面继续决策；同一深度的所有候选完成公平比较后才更新结果。
- 普通／高难两档使用相同时间分配、评分逻辑与计算方案；完成深度受实际剩余时间与状态支持程度影响。
- 普通档按假设概率比较预期收益，并加入不利结果的风险惩罚；高难档的当前根回合固定为已知招式，后续假想回合继续按假设比较。
- 推演的假想局面必须从获准信息和假设构造，不能直接复制真实对局后仅隐藏几个字段。
- 推演使用独立随机源；相同观察、配置与决策种子应能复现决策。

2026-09-08 已根据用户回放加强回复消耗战、无效回血、太晶承伤、盾牌中转、残局铺钉与对手配招推测，并增加实际搜索深度及回退原因统计。代码入口和当前边界见 [AI-FANTASY-STRATEGY.md](AI-FANTASY-STRATEGY.md)；本轮按用户要求未运行测试或对战验收。

同日后续已先编写 [AI-FANTASY-COMPATIBILITY.md](AI-FANTASY-COMPATIBILITY.md)，再实现无效招式换人、Fantasy 异常/回复规则、持久状态恢复、许愿/优质培育位置效果与道具候选推测；该文档区分当前实现、静态检查及尚待用户实战确认的范围。

早期检查发现局面序列化会丢失 `fantasySync` 回调，前期实现已处理相关恢复问题。局面恢复、形态变化及自定义运行状态仍须进行针对性一致性验证，已修复的回调问题不代表任意中盘局面均可完整还原。“恢复后没有报错”不等同于规则一致。

### 服务端与客户端接入

- 服务端增加训练家配置、AI 对局管理、信息视图、策略和推演工作进程。
- 复用现有对战房间与协议；增加明确的 AI 玩家身份及生命周期处理，不能把没有真实连接的 AI 当作断线玩家。
- 新增列出训练家和发起挑战的服务端命令；网页挑战包含所选赛制、训练家标识、难度、请求标识和玩家已提交队伍，服务端拒绝赛制与训练家不匹配的请求。
- 客户端增加赛制选择、训练家选择、难度说明、队伍选择和重新挑战入口，复用现有战斗操作界面。
- 每次决策绑定房间、对局实例和请求编号；过期结果不得提交到新的请求。
- 决策理由仅用于开发调试，首版不向玩家展示。

## 4. 配置、运行与公开使用

### 训练家配置

每名训练家至少包含：稳定标识、显示名称、赛制、固定队伍、战术风格、可选关键成员及资源偏好。

具体代码入口、三个赛制 ID、完整队伍模板、校验与重启步骤见 [AI-FANTASY-TEAMS.md](AI-FANTASY-TEAMS.md)。

队伍采用现有 Showdown 队伍文本导入方式。配置加载时检查赛制结构、队伍人数、招式数量和合法性。错误训练家暂停开放并给出明确诊断，不用随机队伍静默替代。

首套正式配置「幻想先锋·岚」已按用户要求编写，为全员 Fantasy 宝可梦的 FC OU 队伍，包含撒钉手。2026-09-08 配齐四类机制：盔甲鸟携带 Mega 石，兰螳花携带草 Z 提供 Z／气场候选，其余成员保留太晶资格；用户已确认 Z 与气场按现有规则共用一次机会。开发样例仍独立保存，仅在正式配置为空且允许开发内容时回退加载；未通过校验的配置不显示为可用训练家。

### 资源与超时

- 按 1～2 核 CPU、2～4 GB 内存设计。
- 初始默认一个独立推演工作进程、全服最多两场未结束 AI 对局、每名玩家最多一场；这些上限可配置。
- 达到对局上限时拒绝新挑战并提示稍后再试，优先服务已有对局。
- 当前默认 `decisionMs: 10000`、`criticalDecisionMs: 20000`；从收到决策请求开始计算，包含准备、排队和线程启动。同一决策重试不刷新截止点；`null` 仅作为手动开发配置保留。
- 从第十回合起，每局最多两次关键额度，至少间隔十回合，仅按公开残局／强化威胁分配。到截止时提交已有最佳候选，最迟由静态合法选择兜底；详见 [AI-FANTASY-TIME-BUDGET.md](AI-FANTASY-TIME-BUDGET.md)。
- 推演异常、工作进程退出或迟到结果不得阻塞真实对局。

### 玩家等待与断线

玩家不设每回合思考倒计时。断线后保留对局十分钟，期间不替玩家自动出招；重连恢复现有房间和操作请求。

断线满十分钟后关闭对局并释放资源。主动认输按正常对战结束处理。首版不承诺服务端重启后的对局恢复。

### 开放与观测

功能通过服务端开关控制，验收通过后面向全部私服玩家开放，不设置长期测试用户限制。

记录决策耗时、超时、降级、非法行动、工作进程错误、并发数和对局结束原因。调试记录注明信息档位，不把隐藏配置写入公共日志。

## 5. 测试与验收门槛

### 功能与行为

必须覆盖：两档创建对局、训练家与队伍校验、队伍预览、强制换人、特殊机制、认输、重赛、断线重连、十分钟清理、满载拒绝新挑战、显式预算兜底和不限时任务取消。

设置可复现局面测试，验证：

- 优先选择可靠收割，避免攻击已知免疫目标。
- 恢复、强化、换人具有合理触发条件。
- 避免无收益反复换人，能保留关键成员。
- 正确评估气场爆发等特殊资源及互斥机制。
- 普通档可使用全队初始配招，但不读取其他未公开真实配置；高难档还可使用本回合已合法提交的招式，不读取未授权动态状态。
- 改变玩家待执行行动或真实随机源，在 AI 可见信息不变时不影响决策。
- 推演不修改真实对局，复制恢复后的 Fantasy 行为通过针对性一致性验证。

### 强度门槛

采用固定、版本化且不参与调参的评测集。每组对照至少 1,000 局，跨多套队伍、战术类型和随机种子，配对交换控制策略及双方位置，降低配队与位置偏差。

和局按半胜计分：

- **普通档对贪心进攻 AI 的得分率至少 75%。**
- **高难档对普通档的得分率至少 60%。**

贪心基准固定为近似进攻收益评分策略，考虑命中、本系、克制和攻防比例，强制换人选择较有利对位；不使用未来推演或额外隐藏信息。

报告总体与分赛制结果、样本量和置信区间。门槛按总体观测得分率判定，同时列出各赛制薄弱项。强度评测必须报告降级比例，性能测试另外覆盖满载情形。

上述数字是用户确认的开放前目标，当前尚未验证。未达标时继续优化策略，不自动降低门槛，也不能将已有随机 AI 对战通过视为强度达标。

## 6. 正常客户端游玩流程（已确认）

### 用户已明确的体验要求

- 使用正常客户端网页，玩家像平常玩游戏一样进入和操作。
- 玩家应通过页面选择和按钮开始 AI 对战，不需要输入开发命令、粘贴登录断言或运行脚本。
- 赛制、训练家、难度和队伍选择完成后，进入现有真实对战房间，继续使用原有选首发、出招、换人和特殊机制按钮。
- 不以独立 demo 页面、临时脚本注入或替换客户端登录逻辑来代替正式功能。
- 此前创建的 `tmp/fantasy-ai-demo.cjs`、`tmp/fantasy-ai-demo-client.js` 及其附带文件已删除，相关临时服务已停止。
- 客户端和服务端实现均在独立开发分支进行；当前使用 `codex/fantasy-ai`。

### 已确认的玩家流程

2026-09-07，用户已确认以下方案：

| 事项 | 最终选择 | 确认状态 |
|---|---|---|
| AI 对战入口 | 正常客户端首页「队伍编辑器」下方增加「AI 挑战」按钮 | 已确认 |
| 玩家没有队伍时 | 必须先在现有队伍编辑器创建或导入队伍 | 已确认 |
| 体验与交付环境 | 本机打开正常客户端网页，直接操作游玩 | 已确认 |
| 身份要求 | 完全沿用现有客户端的登录和昵称规则 | 已确认 |
| 训练家展示 | 名称、头像、简介、战术风格和赛制 | 已确认 |
| 选择方式 | 同一页面先选 FC UBUU / FC OU / FC UU，再选该赛制的训练家、难度和队伍，最后点「开始挑战」 | 已确认 |
| UI 风格 | 与现有客户端保持一致 | 已确认 |
| 重新挑战 | 返回同一选择页，保留上次设置，允许修改后再开始 | 已确认 |
| 设备范围 | 首期同时适配桌面和手机浏览器 | 已确认 |
| 训练家内容 | 用户可直接提供或要求编写；已按要求加入全 Fantasy 的「幻想先锋·岚」FC OU 队伍与撒钉手 | 已确认 |
| 默认与记忆 | 首次默认普通，之后记住上次赛制、训练家、难度和队伍 | 已确认 |
| 难度说明 | 在难度选项下直接显示简短说明 | 已确认 |
| 未开放时的入口 | 保留首页入口，进入后明确说明当前不可挑战的原因 | 已确认 |

本阶段不提供玩家预设队伍或一键借队。没有队伍时，页面引导玩家进入现有队伍编辑器；完成创建或导入后，回到 AI 挑战入口选择队伍。客户端提交所选队伍后，仍由服务端执行对应 FC 赛制与六对六校验。

本阶段的交付地址为本机正常客户端地址，不以更新线上私服作为完成条件。具体本机启动和资源配置方式须在开发时采用正常客户端支持的方案，不恢复已删除的 demo 文件或临时登录替换逻辑。

### UI 一致性要求

- 首页「AI 挑战」入口采用现有首页按钮的层级、尺寸、间距和交互状态。
- 选择界面复用现有客户端的配色、字体、按钮、表单控件、弹窗或面板规范，不另立一套视觉风格。
- 头像采用客户端已有的训练家头像展示方式；赛制名称、队伍名称与宝可梦图标沿用现有显示规则。
- 难度说明、加载提示、校验失败与错误消息采用现有辅助文字和错误提示样式。
- 已有主题及界面缩放方式继续生效，避免通过独立全局样式改变其他页面。
- 对战阶段直接进入原有战斗界面，保持现有按钮、战斗记录和操作习惯。
- 桌面和手机使用同一套功能与设计规范；小屏下允许自然换行和纵向排列，不能通过横向滚动才能找到选择控件或开始按钮。已有战斗界面的移动端行为继续复用。
- 验收时同时查看现有首页、AI 选择界面和原有对战界面，确认视觉与操作的一致性。

### 单页选择流程

1. 玩家打开本机正常客户端，按现有规则完成所需的登录或昵称步骤。
2. 在首页点击「AI 挑战」，打开统一的选择界面。
3. 先选择 FC UBUU、FC OU 或 FC UU，再选择该赛制下的训练家，在同一界面查看名称、头像、简介、战术风格和对应 FC 赛制。切换赛制会清除不匹配的训练家，不自动代选另一位；玩家队伍保留并在提交时按新赛制校验。挑战前不额外展示 AI 的六只宝可梦或详细配招；进入实际对战后的队伍预览保持原赛制规则。
4. 选择普通或高难难度，并阅读对应的信息权限说明。
5. 从现有队伍编辑器保存的队伍中选择自己的队伍。没有队伍时先进入编辑器创建或导入，再回来继续选择。
6. 点击「开始挑战」，由服务端校验身份、所选赛制与训练家的对应关系、难度、队伍和名额。
7. 创建成功后进入真实对战房间的队伍预览，之后使用原有战斗按钮完成对局。

在当前页面尚未满足必要条件时，应明确指出缺少什么；不能让玩家通过猜测或输入聊天命令补完流程。具体的单页面板或弹窗容器在实现时选择最符合现有客户端习惯的方式，不能把选项拆成已否决的多步向导。

### 重新挑战与训练家内容

对战结束后，点击「重新挑战」回到同一选择界面，带回刚结束对局的赛制、训练家、难度和玩家队伍。玩家可直接保留这些选择，也可修改；只有再次点击「开始挑战」才创建新对局，不自动开战。

重赛仍需重新校验训练家是否开放、队伍是否有效和对局名额。若原训练家已停用或原队伍已删除，应明确指出失效的选择，引导重新选择，不能悄悄换用其他训练家或队伍。保存及观看回放复用现有入口。

正式训练家的名称、头像、简介、战术风格、FC 赛制和固定队伍由用户提供或按其明确要求编写，再按配置校验要求接入。2026-09-07 用户要求编写全 Fantasy 队伍并包含撒钉手，现已加入「幻想先锋·岚」。独立的开发样例仍只用于本机联调，不恢复独立 demo 客户端或登录替换脚本。

### 默认选择与难度说明

- 首次进入时难度默认选择普通，赛制由玩家主动选择。玩家选择赛制、训练家、难度和队伍后，后续重新打开入口时恢复上次选择。
- 选择记忆复用本浏览器的客户端偏好机制，按服务器与玩家身份区分，不新增云端挑战档案或账号战绩。
- 恢复选择时重新检查训练家和队伍是否仍然存在。已删除、停用或无法可靠对应的选择须提示重新选择，不能仅凭过期的列表位置换用另一支队伍。
- 若玩家在队伍编辑器修改了所选队伍，开始挑战时使用当前保存的队伍并重新校验；界面显示应与实际提交一致。
- 从刚结束的对战点击「重新挑战」时，优先恢复该局的选择；玩家仍需点击「开始挑战」确认创建新局。

难度选项下直接显示所选难度的简短说明，使用现有辅助文字样式。建议文案如下，含义为本阶段必须保留的信息：

| 难度 | 简短说明 |
|---|---|
| 普通 | AI 知晓你全队每只宝可梦的配招，并根据公开对战信息进行判断。 |
| 高难 | AI 知晓你全队的初始配置和精确能力值，还会在你提交后读取本回合所选招式。 |

两档使用相同队伍、策略和计算预算；高难按最新授权读取本回合所选招式。首期不增加独立的信息对照弹窗或额外确认步骤。

### 页面状态与异常处理

以下为已确认玩家流程的实现要求；具体文字采用现有客户端的提示风格。

| 状态 | 页面行为 |
|---|---|
| 正在读取训练家 | 显示加载状态，数据未就绪时不能提交挑战 |
| 功能关闭 | 保留首页入口，进入后显示「AI 挑战尚未开放」并禁用开始操作 |
| 没有可用训练家 | 保留首页入口，进入后显示「目前没有可挑战的 AI 训练家」并禁用开始操作 |
| 所选赛制没有训练家 | 明确说明该赛制暂无训练家，允许切换赛制，不自动代选训练家 |
| 无法连接或加载失败 | 明确提示连接或加载问题，提供重试；不误报为没有训练家 |
| 玩家未满足身份要求 | 引导完成正常客户端要求的身份步骤，并保留已有选择 |
| 没有玩家队伍 | 引导进入现有队伍编辑器创建或导入，不能用预设队伍自动开始 |
| 队伍不符合赛制或六对六要求 | 展示实际校验原因和修改队伍入口，保留训练家与难度选择 |
| 正在创建对局 | 显示进度，防止重复点击创建多场；失败后恢复可操作状态 |
| 玩家已有未结束的 AI 对局 | 提供返回原对局的入口，遵守每名玩家的对局限额 |
| 全服 AI 名额已满 | 提示稍后重试；沿用现有拒绝新挑战规则，不额外引入排队机制 |
| 对局中断线 | 告知十分钟保留规则，重连后回到原对局，不替玩家自动出招 |
| AI 对局结束 | 「重新挑战」返回选择页并保留该局设置，同时保留返回与现有回放入口 |

### 接入要求与实现边界

以下是实现已确认体验所需的技术约束，不要求玩家接触接口或配置：

- 复用现有训练家查询和挑战管理逻辑，补齐头像与简介等已确认的展示字段。列表只返回公开摘要；固定队伍、关键成员、资源偏好与高难初始快照继续留在服务端。
- 训练家头像复用现有头像标识与解析方式；简介作为普通文本处理。训练家名称、简介、赛制和队伍名称等内容须按现有客户端方式转义，不能直接拼接成不受控 HTML。
- 玩家选队复用现有队伍存储、选择和提交机制。不能用未确认的默认队伍替代所选队伍，也不能让可疑的本地筛选结果跳过服务端合法性检查。
- UI 必须能够收到明确的对局创建成功或失败结果。成功时关联真实房间，失败时展示原因并恢复操作；网络超时后先确认是否已经创建房间，不能自动重复发起挑战。
- 进入已有 AI 对局和结束后的重赛必须保持玩家身份、房间和训练家的一致性，不能把 AI 当作需要真实账号的真人挑战对象。
- 正常客户端的资源地址、连接地址和身份服务应在交付前配置可用。玩家打开约定地址后只进行正常游戏操作，不承担复制登录数据、手动发协议命令等联调步骤。
- 本阶段仅在本机启动和验收。开发及演示内容不能误发布到现有线上私服；线上开放仍受正式内容和第五节验收门槛约束。

上述接入要求已进入实现阶段。按用户最新指令，本机实际 `config/config.js` 已默认开启 AI 与开发训练家，普通 8000 服务直接提供挑战；不再另起 8001。正式训练家配置仍由用户编辑，正式内容与强度验收状态见进度文档。

### 浏览器验收要求

正常客户端的浏览器操作是本阶段交付条件。自动化逻辑测试可用于开发者验证，但不能代替以下实际玩家流程：

1. 从约定的正常网页地址进入，使用正常身份流程。
2. 通过约定入口选择三个赛制，检查各自的训练家列表与难度说明。
3. 通过界面准备并选择符合赛制的六只队伍。
4. 点击开始，进入真实 AI 对战的队伍预览。
5. 使用现有按钮选择首发并完成多个回合，确认 AI 实际行动及血量、异常、换人等状态更新。
6. 验证认输或正常结束，以及页面中的重新挑战和回放入口。
7. 验证无队伍、队伍不合法、名额占满和断线重连等状态能在页面内处理。
8. 验证首次普通、重新进入后的选择恢复、训练家或队伍失效提示，以及重赛只返回选择页、不自动开始。
9. 验证功能关闭、暂无训练家和连接失败时仍有明确的入口反馈，且无法误创建对局。
10. 在桌面与手机尺寸下完成上述核心流程，对照现有 UI 检查主题、字体、按钮、控件和布局的一致性。

不得用静态画面、预先编排的战斗记录或只通过服务端命令的结果宣称完成上述网页验收。浏览器验收通过也不等于完成第五节的强度验收。

### 待提供内容与开发顺序

本轮玩家流程的选项已确认，不再保留未选择的交互方案。首套正式训练家配置已按用户要求加入，其余训练家和赛制内容可继续提供或配置。当前本机默认 8000 已按用户要求启用 AI；完整网页验收、复杂状态与第五节的强度指标仍待验证，不能将队伍合法性校验视为这些指标已通过。

按用户的继续开发指令，实施顺序如下；完成情况以进度文档为准：

1. 在独立分支补齐训练家公开摘要及客户端需要的挑战结果反馈，保持已有信息隔离与容量限制。
2. 在现有客户端中实现首页入口、同页选择、队伍编辑器引导、默认记忆和完整状态反馈。
3. 将创建成功、已有对局、对战结束后的重赛与回放接入现有战斗界面。
4. 准备正常的本机客户端、连接与身份环境，按浏览器验收要求实际游玩，检查桌面与手机体验。

### 文档与开发的阶段边界

- 当前：已按用户指令开展实现，并完成正常客户端入口与部分实际网页验收。
- 文档调整：用户提出修改时，先更新相关设计及验收要求。
- 运行范围：仅启动本机测试服务；正式内容、完整验收与强度门槛完成前不向线上发布。
