# 幻想杯肉鸽系统接入记录

日期：2026-09-19。范围：第一批系统接入，尚未提供正式可通关的 200 层内容。玩法依据 [FANTASY-ROGUE-DESIGN.md](FANTASY-ROGUE-DESIGN.md)。

## 已接入

- 首页「AI 挑战」下方增加「幻想杯肉鸽」，独立房间提供初始选择、队伍、路线、成长、休整商店与继续入口。
- SQLite 保存账号与冒险；状态版本和请求凭据在同一事务写入。重复提交不重复扣钱、购成长、发奖励或建立对局；过期状态要求刷新。
- 1～200 层固定排布，普通楼层接受三选一内容；休整／首领固定进入。普通战斗节点要求三场一对一野怪战，全部完成才发一份奖励及 1 成长点。
- 原生 Showdown 战斗和 Fantasy AI；服务器从存档组队，不使用编辑器的队伍。HP、PP、异常、倒下和消耗后的持有物在场间延续。
- 球作为引擎队列中的一次行动，先于攻击执行；捕捉失败后对手继续行动，成功结束该场。背包数量与捕捉资格由引擎验证，普通赛制不可使用。
- 捕捉成功立即保存永久次数与初始解锁，同一冒险内同一只野怪跨整层重试去重。配置显式映射捕获形态到最初形态，支持 1 次／10 次门槛。
- 战败免费无限重试整层，恢复入层队伍、背包、货币。服务器重启或房间丢失时，回到该小场开战前检查点；不承诺逐回合断点续战。浏览器刷新可返回仍存活的对局。
- 六维各 +10、单次 1 点；初始栏位 1～6、单次 20 点；同一余额，全部购满共 160 点。开局保存加成快照，后续购买从下局生效。
- 永久加成在性格计算后加入面板，含固定 1 HP 物种；不改变种族值。Fantasy Mega 的 HP 重算、AI 假设重建与后续推演也携带此加成。
- 休整中治疗只恢复存活成员，可反复治疗；购买及使用活力碎片后仍可再次治疗，点击继续才结算休整层。
- 肉鸽与 AI 挑战共享调度器及名额。修复私密房间设置导致房间改名后 AI 调度键失配的问题。

## 主要文件

| 文件 | 职责 |
|---|---|
| `config/fantasy-rogue.ts` | 正式内容入口，当前为 `null`；等待用户提供配置 |
| `server/fantasy-rogue/types.ts` | 内容、账号、冒险、请求类型 |
| `server/fantasy-rogue/content.ts` | 固定楼层排布、内容校验 |
| `server/fantasy-rogue/store.ts` | SQLite 事务、版本与请求去重 |
| `server/fantasy-rogue/engine.ts` | 初始选择、楼层、成长、商店、回退、捕捉账本 |
| `server/fantasy-rogue/manager.ts` | 账号权限、真实对局创建和恢复、客户端状态 |
| `server/chat-plugins/fantasy-rogue.ts` | `/cmd fantasyrogue` 与页面操作请求 |
| `sim/fantasy-rogue.ts` | 私有战斗状态、恢复与导出、投球行动 |
| 客户端 `js/client-fantasy-rogue.js` | 原生房间 UI |
| 客户端 `js/client-battle.js` | 战中精灵球、结束后继续冒险 |

## 正式内容的填写顺序

1. 初始池：每个物种提供 5 级配置，包含特性、性格、招式、个体值／努力值，以及初始是否可用。
2. 初始货币和背包；球、复活和治疗道具的名称、价格、数量／治疗量。
3. 第一批楼层：普通层三个节点，每个野怪节点三只对手；第 9 层中心、第 10 层首领。按同样接口扩展至 20 层及后续阶段。
4. 可捕捉对象的最初形态映射与门槛。捕捉概率目前是按对局及球种显式配置的 0～1 概率；最终 HP／异常修正公式确定后替换计算接口，不把测试概率当成正式平衡。
5. 后续再定经验曲线、进化和学招、幻想强化、掉落素材和事件。当前系统没有擅自发经验或自动进化。

配置符合 `RogueContent` 类型，必须有版本号及第一层。后续未配置楼层会暂停并保存进度，不用自动胜利填补。修改存量阵容和经济规则时应更新版本；旧冒险版本不匹配时拒绝写入，需恢复原版本或明确迁移。仅追加未来未提供楼层可保留版本。

正式进度绑定注册账号。`Config.fantasyrogue` 可设置 `enabled`、`database`；默认数据库为 `databases/fantasy-rogue.db`，需要运行环境可用的 `better-sqlite3`。本次未修改实例的 `config/config.js`，未启动正式生产模式或部署。

## 验证与本地测试

服务端构建：`node build`。客户端构建：在客户端仓库执行 `node build`。客户端现有 PHP 新闻生成提示不影响构建。

定向服务端：

```powershell
node node_modules/mocha/bin/mocha.js --no-config test/main.js test/server/fantasy-rogue.js test/server/fantasy-rogue-online.js test/server/fantasy-ai-information.js --timeout 20000 --reporter dot --exit
```

客户端：

```powershell
node node_modules/mocha/bin/mocha.js --no-config test/fantasy-rogue.test.js test/fantasy-ai.test.js --reporter dot
```

本地浏览器测试配置：服务端执行 `node test/fixtures/fantasy-rogue-local.js`，客户端执行 `node build-tools/serve-local.js`，打开 `http://localhost:8080/`。这个明确命名的测试入口仅监听回环地址，使用内存数据库和标注「测试」的队伍／球，允许测试用户名；不会修改正式内容文件或生产存档。关闭测试进程后测试存档丢弃。

已验证：定向服务端 27 项、客户端 21 项测试通过，双方构建通过。另以 SQLite 写入故障验证成长扣点和请求凭据共同回滚，恢复后重试仅扣点一次。扩大回归的两个组合分别为 53 项通过／1 项失败、74 项通过／1 项失败。两项失败分别是旧测试预期 `development-balanced` 而当前正式训练家为 `acelora-ou`，以及既有重复换人惩罚断言。原有信息权限文档已记录并验证过这两项基线问题，本次未改正式训练家配置或重复换人策略。

新增肉鸽模块、客户端页面与受影响 AI 文件的定向 ESLint 通过。扩大到模拟器及格式文件时有原有缩进等诊断，按修改行核对并修正了本次新增问题；没有批量改写旧文件格式。全库 TypeScript 仍有六项错误，集中在旧的 Gen 8／SSB／Gen 7 random teams／othermetas 对 Mega 返回值类型的使用；肉鸽及本次修改路径没有新增报错。本机实际安装的 TypeScript 为 5.7.3，因此运行检查时用 `--ignoreDeprecations 5.0` 覆盖仓库为 6.0 准备的选项，未更改全局编译配置。

真实浏览器已从首页建立测试冒险，进入原生战斗，验证失败投球后野怪继续出招、成功捕捉后返回第二场，背包减少、捕获个体的剩余 PP 保存、小场不提前发点；刷新后重新使用同一测试用户名，第二场进度、队伍和背包仍正确。测试进程已关闭。完整 200 层客户端通关未验收，正式内容尚未配置。

## 仍待处理

- 满六只队伍后的捕捉／替换规则待确认；当前入口保护不会让存档超过六只。
- 经验、学招、进化、幻想碎片与技能池事件、分支增益、稀有事件及素材掉落，待规则和内容补齐后继续实现。
- 特殊机制的跨场资源规则未最终确定；当前使用原生的每场规则，临时形态不写入永久存档。
- 未移植 PokéRogue 源码或导入素材；这批界面使用已有 PS 图标，后续新增缺失道具贴图时按设计文档记录来源和许可。
