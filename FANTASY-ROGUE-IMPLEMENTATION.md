# 幻想杯肉鸽试玩版开发记录

更新：2026-09-19。当前交付为 200 层测试内容和基础养成，供试玩调参；正式阵容和经济数值后续替换。依据见 [设计文档](FANTASY-ROGUE-DESIGN.md)，来源见 [数据与许可说明](FANTASY-ROGUE-DATA.md)。

## 本地试玩

两边均在 `codex/fantasy-rogue` 开发。服务端 `master` 保持 `ffbaf54da`，客户端原 `fantasy` 保持 `bbd1b7e8`；未合并、未推送、未部署。迁移前的 stash 备份保留。

先完整构建：

```powershell
cd F:\fantasy\pokemon-showdown-client
node build full --local-server ..\pokemon-showdown
```

该命令构建旁边的服务端并生成客户端数据，不执行 Git 拉取，解决原 `node build full` 因 GitHub 443 不通而提前失败的问题。两边需要已安装依赖；未提供 `--local-server` 时保留原远端缓存流程。PHP 新闻提示不阻止编译。

另开两个终端，分别启动：

```powershell
cd F:\fantasy\pokemon-showdown
node tools/fantasy-rogue-preview.js
```

```powershell
cd F:\fantasy\pokemon-showdown-client
node build-tools/serve-rogue-preview.js
```

打开 **http://localhost:18080/**，输入固定测试昵称，点击首页「AI 挑战」下方的「幻想杯肉鸽」。首次任选一只御三家；逐层点数可购买下局生效的初始栏位与六维加成。

试玩对战仅监听 `127.0.0.1:18000`，网页仅监听 `127.0.0.1:18080`，无需停用现有 8000 私服。试玩存档为 `databases/fantasy-rogue-preview.db`，以测试昵称绑定；关闭进程不清空存档，之后用同一昵称继续。只有显式本地模式接受测试昵称，生产认证规则及 `config/config.js` 未修改。试玩 AI 决策预算 1 秒、关键决策 1.5 秒，实际私服继续使用自身配置。

不要重复启动占用中的端口。切换分支后重新构建，避免混用另一分支的生成产物。旧的 `test/fixtures/fantasy-rogue-local.js` 仍是内存存档的少量测试夹具，不用于本次试玩。

## 测试内容与调参

`config/fantasy-rogue.ts` 加载 `server/fantasy-rogue/preview-content.ts`，版本 `preview-2026-09-v2`。这是用户授权的测试配置，不代表正式平衡。

| 项目 | 当前测试值 |
|---|---|
| 初始伙伴 | 1～9 世代 27 只御三家；5 级、中性性格、20 IV、0 EV |
| 初始资源 | 1500 金币、精灵球 12、伤药 6、活力碎片 1、PP 补剂 1、XS 糖果 3 |
| 普通野怪 | 10 区各 8 条候选进化链；15 IV、0 EV；每层三场单敌战 |
| 野怪等级 | min(100, max(3, 2 + ceil(层数 / 2))) |
| 精英 | 同层等级 +5，上限 100；31 IV；后段包含可捕捉的传说／幻之宝可梦 |
| Boss 等级 | min(100, 5 + ceil(层数 / 2))；25 IV，队伍从 1 只增至 6 只 |
| 野怪层收益 | 150 + 12 × 层数金币，每三层附伤药 |
| Boss 收益 | 800 + 40 × 层数金币、超级球 3、活力碎片 1 |
| 球价／捕捉倍率 | 精灵球 200／1，超级球 600／1.5，高级球 1200／2 |
| 治疗 | 伤药 150／20 HP，好伤药 500／60 HP，厉害伤药 1200／120 HP |
| 其他补给 | 活力碎片 1000／半血复活；万灵药 400；PP 补剂 800／各招 +10 |
| 糖果价格／经验 | XS 100／100，S 600／800，M 1800／3000，L 5000／10000 |
| 进化道具 | 进化石 1200，联系绳 2000，暂代通信进化 |

普通层有两条不同区域的野怪路线，第三项轮换补给箱、训练家或精英。测试内容是可重复的固定表，便于调参与重试去重，未实现随机地图和正式事件系统。

Boss 全部已配置：10/30/…/170 精英首领，20/40/…/160 八道馆；165/175/180/185 四天王；190 冠军；200 幻想超梦。所有 Boss 前一层固定休整，无三选一。完整临时名单见 `PreviewBosses`，并非正式馆主及八时期阵容。

调参位置：`PreviewBalance` 管开局和等级，`PreviewBiomes` 管野怪，`PreviewBosses` 管队伍，`content.items` 管价格及效果，各节点 `reward` 管金币和糖果掉落。修改阵容和经济时更新内容版本；存量冒险版本不匹配时停止写入，需保留旧版本或明确迁移。

## 原作机制的试玩基线

- 六种原作成长曲线；第七世代等级差经验公式。参与者各获完整经验，其他符合条件者各获一半，不按人数瓜分；按对手被击倒时仍存活的成员确定资格。成功捕捉同样给经验。玩家和对手暂限 100 级。
- 物种基础经验、捕获率及经验表来自固定 PokéAPI 快照；学招／普通进化使用普通 Gen 9 Dex，取该物种已有的最新世代等级招式来源，不提前开放 Fantasy 专属技能池。
- 在小场胜利后结算经验和处理进化／学招，非战内即时升级。层末须处理完待学招式和捕获安置，才建立下一层快照。未增加交换、亲密、幸运蛋或环境经验奖励，也未增加对战 EV 获取。
- 升级保留伤害、异常和剩余 PP；最大 HP 增量只补差额，不让倒下者复活。满四招时选择替换或跳过；单一普通等级进化自动进行，进化石与联系绳在场外点选进化。特殊亲密度、地点、时间、同行条件等尚未实现。
- 捕捉采用第六／七世代常规球公式，按物种捕获率、当前 HP、异常、球倍率计算四次摇晃；睡眠／冰冻 ×2.5，麻痹／中毒／剧毒／灼伤 ×1.5。会心捕捉依据实际捕获物种数，塔内不加入草丛或洛托姆修正，不是统一固定百分比。
- 投球占一次行动，失败后野怪出招，成功结束小场。满队暂允许捕捉，再替换一只或放走新成员，两者均保留永久捕捉记录。普通首次捕捉解锁最初形态，单阶段传说／幻之宝可梦第 10 次解锁。
- 原作机制有代际差异，上述为已经说明的可调整试玩基线，100 级上限与满队处理仍可调整。数据和公式不声称逐位复刻某款原版 ROM。

## 存档与战斗

服务端生成队伍，使用原生 Showdown 画面与既有 Fantasy AI 调度、普通档信息边界。HP、PP、异常、倒下和消耗后的持有物跨场保存。休整不限次数恢复存活成员，复活后仍可再治疗，由玩家点击继续。

战败免费无限重试整层，回退入层的队伍、经验、招式、进化、背包和金币，整层完成才发 1 成长点。捕捉即时永久入账，同一冒险／楼层／路线／小场的对象跨重试去重。SQLite 事务保存请求凭据和状态，重复结算不重复奖励。满队安置和学招选择也可刷新续办。

服务器重启／房间丢失恢复至该小场开战前，浏览器刷新可回到尚在运行的对局。永久六维各最多 +10，每 +1 花 1 点；栏位 1→6、每个 20 点，共用余额，新购买从下局生效，升级或重建不重复叠加。

## 文件

| 文件 | 用途 |
|---|---|
| server/fantasy-rogue/preview-content.ts | 可替换的 200 层内容 |
| server/fantasy-rogue/progression.ts | 经验、学招、进化 |
| sim/fantasy-rogue-data.ts、fantasy-rogue-rules.ts | 离线数据与公式 |
| server/fantasy-rogue/engine.ts、store.ts | 楼层、背包、账目和事务 |
| server/fantasy-rogue/manager.ts、sim/fantasy-rogue.ts | 原生对战与结算 |
| 客户端 js/client-fantasy-rogue.js | 队伍、背包、学招、进化与替换 |
| 客户端 sprites/fantasy-rogue/README.md | PokéRogue 道具图集来源及声明 |

## 验证

服务端：

```powershell
node node_modules/mocha/bin/mocha.js --no-config test/main.js test/server/fantasy-rogue.js test/server/fantasy-rogue-online.js test/server/fantasy-rogue-preview.js test/server/fantasy-ai-information.js --timeout 20000 --reporter dot --exit
```

37 项通过，包括真实引擎参与／击倒记录、经验结算幂等、捕捉、满队释放和替换、糖果进化、伤害／PP／异常延续、等级上限、学招后的跨层快照，以及 200 层固定节点与点数。200 层检查注入胜利结果验证状态机，不代表 AI 实战或真人完整通关。

客户端：`node node_modules/mocha/bin/mocha.js --no-config test/fantasy-rogue.test.js test/fantasy-ai.test.js --reporter dot`，23 项通过。完整本地构建成功；新增模块与客户端修改的定向 ESLint 通过。

全库 TypeScript 仍有六项既有 Mega 返回值类型错误，位于 Gen 8 rulesets、Gen 9 SSB、Gen 7 random teams、othermetas；本次路径无新增错误。本机 TypeScript 5.7.3 检查使用 `--ignoreDeprecations 5.0`，未改全局设置。上一阶段的扩大 AI 回归还记录过正式训练家 ID 和重复换人惩罚的两项旧断言问题，本轮未修改相关策略。

浏览器使用普通试玩内容，实际完成第 1 层连续三场战斗：糖果 5→9 级与满招替换；投球失败后野怪行动、成功捕捉；击倒与捕捉经验、全队分享；捕获者的剩余 HP／PP；进入第 2 层后恰好获得 1 点；刷新并重新输入同一昵称恢复存档。另以单独标记、直接设置为第 9 层的 QA 存档验证中心：存活者 1→20 HP，倒下者仍为 0；花 1000 购买活力碎片、复活至 9/19、再次治疗至 19/19，点击继续固定进入第 10 层 Boss。中心专项存档不计作自然通关。实看了中文队伍及道具图集的显示。

未完成 200 层真人通关及难度验收，不能把状态机测试或上述专项存档当作完整实战证明。启动后已核验 8000 原私服进程仍在运行，试玩独立监听回环 18000／18080。

## 尚未完成的正式版内容

幻想碎片及专属技能池事件、构筑增益、支线奇遇、素材掉落、完整特殊进化和招式回忆仍待实现。临时特殊战斗机制按原生每场规则，临时形态不写为永久形态。后续需完整客户端通关与经济／难度调参，再按用户授权合并、部署。
