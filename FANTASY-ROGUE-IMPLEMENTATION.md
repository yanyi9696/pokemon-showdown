# 幻想杯肉鸽试玩版开发记录

更新：2026-09-25。当前交付为 200 层测试内容和基础养成，九个幻想精英池已替换为用户最新的 54 套配置，每层六个候选。太晶权限按事件与楼层限制；战败后治疗并继续当前场次，支持撤退与 5000 金币急救，招式使用基础 PP。其他正式阵容和经济数值后续替换。依据见 [设计文档](FANTASY-ROGUE-DESIGN.md)，来源见 [数据与许可说明](FANTASY-ROGUE-DATA.md)。

## 本地试玩

两边均在 `codex/fantasy-rogue` 开发。服务端 `master` 保持 `ffbaf54da`，客户端原 `fantasy` 保持 `bbd1b7e8`；肉鸽开发仍在独立分支，未合并到原分支。迁移前的 stash 备份保留。

先完整构建：

```powershell
cd F:\fantasy\pokemon-showdown-client
node build full --local-server ..\pokemon-showdown
```

该命令构建旁边的服务端并生成客户端数据，不执行 Git 拉取，解决原 `node build full` 因 GitHub 443 不通而提前失败的问题。两边需要已安装依赖；未提供 `--local-server` 时保留原远端缓存流程。PHP 新闻提示不阻止编译。

本地开发入口直接打开客户端文件，连接 `localhost:8000`。只输入昵称试玩时，必须使用本地试玩启动命令。先停止原来占用 8000 的普通私服；若旧 18000 试玩服仍在运行，也先停止，避免两个试玩进程同时使用同一份存档。另开终端运行：

```powershell
cd F:\fantasy\pokemon-showdown
npm run start:fantasy-rogue:local
```

在浏览器地址栏打开（`~~` 前不加反斜杠）：

```text
file:///F:/fantasy/pokemon-showdown-client/play.pokemonshowdown.com/testclient.html?~~localhost:8000
```

输入固定测试昵称后，点击首页「AI 挑战」下方的「幻想杯肉鸽」。首次任选一只御三家；每战胜一个 Boss 获得 1 点；仅在无进行中冒险时可购买初始栏位与六维加成。`testclient.html` 已引入肉鸽脚本与样式，道具图集使用相对路径，直接打开文件也能定位资源。修改 HTML、CSS 或客户端原生 JS 后刷新页面即可；修改需编译的代码时先重新构建。更新服务端代码后需重启现有服务。

该命令先构建服务端，再以 `node tools/fantasy-rogue-preview.js --port 8000` 启动；仅监听 `127.0.0.1`，使用此前 18000 试玩服的 `databases/fantasy-rogue-preview.db`，同一昵称可继续原来的试玩进度。此入口不需要运行 18080 网页服务。不要同时启动两个使用该数据库的试玩进程。

若使用 `node .\pokemon-showdown` 启动，则仍是普通私服模式：肉鸽要求通过账号密码验证的注册账号，仅输入昵称不符合条件；普通存档默认在 `databases/fantasy-rogue.db`。本地试玩启动器不修改 `config/config.js`、普通服务的认证规则或普通存档。

### 可选：独立试玩服

需要用任意固定测试昵称、与 8000 私服分开试玩时，仍可另开两个终端，分别启动：

```powershell
cd F:\fantasy\pokemon-showdown
node tools/fantasy-rogue-preview.js
```

```powershell
cd F:\fantasy\pokemon-showdown-client
node build-tools/serve-rogue-preview.js
```

打开 **http://localhost:18080/**，输入固定测试昵称，点击首页「AI 挑战」下方的「幻想杯肉鸽」。这是可选的独立环境，日常开发默认使用上方 `file://` 入口。

试玩对战仅监听 `127.0.0.1:18000`，网页仅监听 `127.0.0.1:18080`，无需停用现有 8000 私服。试玩存档为 `databases/fantasy-rogue-preview.db`，以测试昵称绑定；关闭进程不清空存档，之后用同一昵称继续。只有显式本地模式接受测试昵称，生产认证规则及 `config/config.js` 未修改。试玩 AI 决策预算 1 秒、关键决策 1.5 秒，实际私服继续使用自身配置。

不要重复启动占用中的端口。切换分支后重新构建，避免混用另一分支的生成产物。旧的 `test/fixtures/fantasy-rogue-local.js` 仍是内存存档的少量测试夹具，不用于本次试玩。

## 测试内容与调参

`config/fantasy-rogue.ts` 加载 `server/fantasy-rogue/preview-content.ts`，版本 `preview-2026-09-v4`。10～170 层的九个幻想精英池已采用用户 2026-09-25 提供的 54 套配置；其他敌队、经济及掉落仍为用户授权的测试配置。

| 项目 | 当前测试值 |
|---|---|
| 初始伙伴 | 1～9 世代 27 只御三家；5 级、中性性格、20 IV、0 EV |
| 初始资源 | 1500 金币、精灵球 12、伤药 6、活力碎片 1、PP 补剂 1、XS 糖果 3 |
| 普通野怪 | 10 区各 8 条候选进化链；15 IV、0 EV；每层三场单敌战 |
| 野怪等级 | min(100, max(3, 2 + ceil(层数 / 2))) |
| 精英 | 同层等级 +5，上限 100；31 IV；后段包含可捕捉的传说／幻之宝可梦 |
| 幻想精英 | 10/30/…/170 层，等级 10/20/…/90；每层六个候选，等概率抽取一只（各 1/6） |
| 其他 Boss 等级 | min(100, 5 + ceil(层数 / 2))；25 IV，队伍从 1 只增至 6 只 |
| 野怪层收益 | 150 + 12 × 层数金币，每三层附伤药 |
| Boss 收益 | 800 + 40 × 层数金币、超级球 3、活力碎片 1 |
| 球价／捕捉倍率 | 精灵球 200／1，超级球 600／1.5，高级球 1200／2 |
| 治疗 | 伤药 150／20 HP，好伤药 500／60 HP，厉害伤药 1200／120 HP |
| 其他补给 | 活力碎片 1000／半血复活；万灵药 400；PP 补剂 800／各招 +10 |
| 糖果价格／经验 | XS 100／100，S 600／800，M 1800／3000，L 5000／10000 |
| 进化道具 | 进化石 1200，联系绳 2000，暂代通信进化 |

普通层有两条不同区域的野怪路线，第三项轮换补给箱、训练家或精英。普通层仍使用固定测试表；幻想精英在入层时由服务器等概率抽取并持久化，刷新、重连、重复请求、同场再战和连续挑战整层重试保持同一只对手。未实现随机地图和正式事件系统。

Boss 全部已配置：10/30/…/170 幻想精英，20/40/…/160 八道馆；165/175/180/185 四天王；190 冠军；200 幻想超梦。所有 Boss 前一层固定休整，无三选一。用户指定的精英配置见 `server/fantasy-rogue/elite-bosses.ts`；其余临时名单见 `PreviewBosses`，并非正式馆主及八时期阵容。

调参位置：`PreviewBalance` 管开局和普通等级，`PreviewBiomes` 管野怪，`FantasyEliteBosses` 管九个幻想精英池及明确等级，`PreviewBosses` 管其余队伍，`content.items` 管价格及效果，各节点 `reward` 管金币和糖果掉落。修改阵容和经济时更新内容版本；存量冒险版本不匹配且未显式声明兼容时停止写入。

### 幻想精英与太晶权限（阵容更新于 2026-09-25）

- 54 套配置保留用户的物种、特性、招式顺序、明确性别及逐只指定的 0 攻击／0 速度 IV。本版全部无携带道具，波士可多拉改为 110 层、食土、0 速度 IV，取消原超级石。未指定的 IV 为 31、EV 为 0、性格为 Hardy（无修正）；未指定性别按物种比例在入层时生成并保存，重试不改变。未指定太晶属性沿用幻想图鉴 `defaultTeraType`。
- 对手仅在 10、30、50、70、90、110、130、150、170 层的固定幻想精英 Boss 战获得太晶权限。普通野怪、可捕捉精英、训练家、道馆、四天王、冠军和 200 层最终 Boss 均关闭。已有 Mega／Z／太晶互斥仍有效；已保存的旧版携带超级石对手继续遵循原生互斥。
- 玩家每局默认锁定，存档字段 `teraUnlocked` 缺失也按锁定处理。未来事件由服务端调用 `RogueEngine.unlockTerastallization(userid)` 为本局全队解锁；没有浏览器解锁命令，也未虚构尚未设计的触发事件。当前测试版自然游玩尚不能解锁玩家太晶。
- 解锁范围暂按本局全队、每场一次处理，沿用原生战斗次数；普通战败和撤退保留解锁。仅特殊连续挑战整层重试恢复入层许可。新冒险重新锁定。队伍页展示状态，原生招式请求和服务端执行同时校验。
- AI 仅从自己的请求获知自己的权限；推演重建仍禁用其无权使用的太晶。玩家的事件状态不加入 AI 对手初始快照或观察，也不复制完整冒险存档。

## 原作机制的试玩基线

- 六种原作成长曲线；第七世代等级差经验公式。参与者各获完整经验，其他符合条件者各获一半，不按人数瓜分；按对手被击倒时仍存活的成员确定资格。成功捕捉同样给经验。玩家和对手暂限 100 级。
- 物种基础经验、捕获率及经验表来自固定 PokéAPI 快照；学招／普通进化使用普通 Gen 9 Dex，取该物种已有的最新世代等级招式来源，不提前开放 Fantasy 专属技能池。
- 在小场胜利后结算经验和处理进化／学招，非战内即时升级。层末须处理完待学招式和捕获安置，才建立下一层快照。未增加交换、亲密、幸运蛋或环境经验奖励。击败对手增加努力值，单项 252／总计 510，按击倒时存活、有经验资格的全队成员发放完整产出；捕捉只发经验。
- 升级保留伤害、异常和剩余 PP；最大 HP 增量只补差额，不让倒下者复活。满四招时选择替换或跳过；单一普通等级进化自动进行，进化石与联系绳在场外点选进化。特殊亲密度、地点、时间、同行条件等尚未实现。
- 捕捉采用第六／七世代常规球公式，按物种捕获率、当前 HP、异常、球倍率计算四次摇晃；睡眠／冰冻 ×2.5，麻痹／中毒／剧毒／灼伤 ×1.5。会心捕捉依据实际捕获物种数，塔内不加入草丛或洛托姆修正，不是统一固定百分比。
- 投球占一次行动，失败后野怪出招，成功结束小场。满队暂允许捕捉，再替换一只或放走新成员，两者均保留永久捕捉记录。普通首次捕捉解锁最初形态，单阶段传说／幻之宝可梦第 10 次解锁。
- 原作机制有代际差异，上述为已经说明的可调整试玩基线，100 级上限与满队处理仍可调整。数据和公式不声称逐位复刻某款原版 ROM。

## 存档与战斗

服务端生成队伍，使用原生 Showdown 画面与既有 Fantasy AI 调度、普通档信息边界。HP、PP、异常、倒下和消耗后的持有物跨场保存。休整不限次数恢复存活成员，复活后仍可再治疗，由玩家点击继续。

普通战败自动返回场外，全队保持濒死，保留当前小场序号和全部消耗；治疗后重新进入该场。战斗内及冒险页的「撤退」均要求服务端终止并接收最终队伍快照，保存玩家 HP、PP、异常和道具消耗后返回；对手下次恢复本场初始状态，配置不重抽。已完成小场的成长和捕获保留，未通过的本场不发经验、EV 或奖励。

全队濒死、没有复活道具且允许场外治疗时，可以支付固定 **5000 金币**急救，复活全队并恢复全部 HP、已学招式 PP 和异常状态；不补回道具、不改变场次。余额不足时按钮禁用并显示差额。事务保证重复请求只扣一次。特殊节点可设置 `noHealing: true`，开战后直到整层结束禁止场外补给、进化和急救；撤退不解除限制，全灭后才使用入层快照免费重试整层。默认试玩节点未启用该限制。

仅 Boss 层完成发 1 成长点（200 层共 23 点），其他层不发点。捕捉即时永久入账，同一冒险／楼层／路线／小场的对象跨尝试去重。SQLite 事务保存请求凭据和状态，重复结算不重复奖励。满队安置和学招选择也可刷新续办。

所有肉鸽成员（包括对手）使用未经 PP 提升的 `move.pp`，普通格式仍按原 Showdown 规则。旧存档自动移除额外上限并保留已用次数，例如 51/56 → 30/35，最低为零；队伍、招式记忆、待安置捕获者和检查点一起迁移，重复读取不再扣 PP。旧版 `failed` 普通层无法找回当时已丢弃的战败快照，因此保守将保存队伍置为全队濒死，并保留原场次进入补给流程。

服务器重启／房间丢失恢复至该小场开战前，浏览器刷新可回到尚在运行的对局。永久六维各最多 +10，每 +1 花 1 点；栏位 1→6、每个 20 点，共用余额，冒险中禁止购买、无进行中冒险才开放，新购买从下局生效，升级或重建不重复叠加。

## 队伍编辑与收益展示（2026-09-19）

- 路线及楼层待处理操作移到永久加成上方。展示预计奖励与上一层实际到账数量；金币余额在背包，物品以图标卡片分隔。
- 成员标签、队伍前后移动、技能顺序调整、携带物装备与卸下均由服务器校验。道具仅来自背包；非商店配置的合法携带物可归还及再次装备，不会成为零价商品。
- `moveMemory` 保存每个已解锁招式的剩余／基础最大 PP，更换技能不回满。旧存档已遗忘但无 PP 记录的技能保守置 0，尚待学习的新招式使用基础满 PP。中心仅恢复存活成员，包括未装备的已学招式；急救另按上述条件复活全队。
- `abilityPool` 记录已知特性和隐藏标记。`RogueEngine.unlockAbility()`、`unlockEVRespec()` 供未来服务端事件调用；当前没有触发事件，也不提供浏览器解锁命令。努力值重分配须保留已有总量，单项不超过 252、总计不超过 510，一次授权保存后消耗。
- 对战 EV 取固定 PokéAPI 物种产出，Fantasy 形态缺少独立条目时回退基础物种。普通战败和撤退保留此前小场的经验、EV、招式记忆、事件许可及队伍调整，入层回退仅用于特殊连续挑战。
- 招式详情使用 `Dex.mod("gen9fantasy")` 的名称、属性、分类、威力、命中、基础 PP 与简介，配现有中文翻译和 PS 图标。首页入口采用静态星晶配色，不增加动画。
- 大厅仅在创建新冒险时通知一次，重复请求及刷新不通知；肉鸽各场战斗从普通开战广播中排除。

### 本次兼容策略

本次升级为 `preview-2026-09-v4`，明确兼容 `preview-2026-09-v2` 与 `preview-2026-09-v3`。旧存档在下一次成功操作时更新版本，保留队伍、道具、点数、入层快照和已选定的当前对手；进入后续幻想精英层时才使用新池。旧存档未获得事件解锁时，玩家太晶仍默认锁定。重启本地服前确认无进行中战斗并备份。未知版本仍拒绝继续，不自动清档。原队伍改版保留的旧点数不追溯扣除。

## 文件

| 文件 | 用途 |
|---|---|
| server/fantasy-rogue/preview-content.ts | 可替换的 200 层内容 |
| server/fantasy-rogue/elite-bosses.ts | 用户指定的九个幻想精英池、等级与逐只配置 |
| server/fantasy-rogue/progression.ts、team.ts | 经验、努力值、进化、招式记忆及队伍编辑 |
| sim/fantasy-rogue-data.ts、fantasy-rogue-evs.ts、fantasy-rogue-rules.ts | 离线数据与公式 |
| server/fantasy-rogue/engine.ts、store.ts | 楼层、背包、账目和事务 |
| server/fantasy-rogue/manager.ts、sim/fantasy-rogue.ts | 原生对战与结算 |
| 客户端 js/client-fantasy-rogue.js | 队伍、背包、学招、进化与替换 |
| 客户端 sprites/fantasy-rogue/README.md | PokéRogue 道具图集来源及声明 |

## 验证

服务端：

```powershell
node node_modules/mocha/bin/mocha.js --no-config test/main.js test/server/fantasy-rogue.js test/server/fantasy-rogue-online.js test/server/fantasy-rogue-preview.js test/server/fantasy-rogue-team.js test/server/fantasy-rogue-bosses.js test/server/fantasy-ai-information.js test/server/fantasy-ai-rollout.js test/sim/misc/default-tera-type.js test/sim/misc/terastal.js --timeout 20000 --reporter dot --exit
```

2026-09-20 相关回归共 120 项通过：首次合并运行 119 项通过，一项旧测试仍只统计拆分后的非精英临时表（14），修正为实际固定 Boss 总数 23 后，重跑所属 10 项全部通过。最终增加未指定性别的入层保存后，精英及试玩专项 20 项再次通过。覆盖 42 套指定精英的真实模拟器入场、41 套太晶及 Aggronite Mega、太晶开关与非法指令、玩家事件解锁和回退、旧版本兼容、随机候选固定、原有肉鸽机制、AI 信息边界／推演和普通太晶规则。200 层检查注入胜利结果验证状态机，不代表 AI 实战或真人完整通关。

客户端：`node node_modules/mocha/bin/mocha.js --no-config test/fantasy-rogue.test.js test/fantasy-ai.test.js --reporter dot`，30 项通过。服务端及客户端本地构建成功；新增模块与客户端修改的定向 ESLint 通过。`sim/battle-actions.ts` 有既有 4 个缩进错误和 1 条行长警告，已用 HEAD 原文与工作区对比确认完全一致，本次三个新增行未引入 lint 问题。

入口切换补充核验：`node build --local-server ..\pokemon-showdown` 成功生成包含肉鸽脚本和样式的 `testclient.html`；修复已写入 `testclient.template.html`，后续构建可保留。客户端 23 项回归通过，直接连接 `localhost:8000` 的肉鸽状态接口返回 `enabled: true`，未登录时提示登录注册账号。所需肉鸽本地资源存在，原有两份动画尺寸数据仍使用页面已有的官方在线回退。内置浏览器的 URL 安全策略阻止自动打开 `file://`，本轮未实测该入口的页面和战斗；下述浏览器实测来自此前独立试玩环境。

昵称试玩补充修正：原 8000 以普通私服模式运行，输入昵称只获得未注册身份，不能读取普通肉鸽存档。新增 `npm run start:fantasy-rogue:local` 在 8000 使用已有的本地试玩模式和独立试玩存档；生产认证条件未放宽。页面监听服务端确认登录的事件，同一昵称完成账号验证后也会重新读取存档。构建和定向 ESLint 通过，客户端 24 项测试、服务端真实房间 4 项测试通过，包括未注册用户在普通模式被拒、本地模式可试玩、未命名访客被拒及注册账号可读取存档。仅更新代码不会改变正在运行的模式，实际服务切换核验如下。

用户明确授权中断现有服务后，已停止原 8000 普通服和旧 18000 试玩服，并在 `127.0.0.1:8000` 启动本地试玩；18000 不再监听。切换前对普通和试玩 SQLite 数据库分别创建了 `databases/*-before-8000-2026-09-19T13-07-42-852Z.db` 一致性备份。通过真实 WebSocket 使用专用 QA 昵称、无密码登录，成功读取内容、创建第 1 层冒险，断线重连后恢复相同冒险 ID 和修订号，数据库中也保存了对应记录；切换前已有试玩账号和请求收据逐项比对一致。此验证通过对战服务接口完成，未声称浏览器已实际打开 `file://` 页面。客户端需刷新原 `testclient.html` 页面并重新输入固定昵称。

全库 TypeScript 仍有六项既有 Mega 返回值类型错误，位于 Gen 8 rulesets、Gen 9 SSB、Gen 7 random teams、othermetas；本次路径无新增错误。本机 TypeScript 5.7.3 检查使用 `--ignoreDeprecations 5.0`，未改全局设置。上一阶段的扩大 AI 回归还记录过正式训练家 ID 和重复换人惩罚的两项旧断言问题，本轮未修改相关策略。

上一版的浏览器验证：使用普通试玩内容，实际完成第 1 层连续三场战斗：糖果 5→9 级与满招替换；投球失败后野怪行动、成功捕捉；击倒与捕捉经验、全队分享；捕获者的剩余 HP／PP；旧版进入第 2 层后获得 1 点（此规则现已被仅 Boss 发点替代）；刷新并重新输入同一昵称恢复存档。另以单独标记、直接设置为第 9 层的 QA 存档验证中心：存活者 1→20 HP，倒下者仍为 0；花 1000 购买活力碎片、复活至 9/19、再次治疗至 19/19，点击继续固定进入第 10 层 Boss。中心专项存档不计作自然通关。实看了中文队伍及道具图集的显示。

未完成 200 层真人通关及难度验收，不能把状态机测试或上述专项存档当作完整实战证明。此段为上一版独立 18000／18080 环境的历史记录；当前试玩已迁至回环 8000。

### 本次队伍改版验证

本次队伍改版实测：通过独立的内存 QA 服务（18001）和 HTTP 客户端（18081），实看并操作了换招／换回后 PP 保持 3/40、已解锁隐藏特性选择、携带物装备与归还、队伍前后移动和待学招式面板。QA 直接设置的 Boss 待结算存档在完成选择后，显示金币 +1200、超级球 ×3、活力碎片 ×1、成长点 +1；该测试不代表实际打败 Boss。420 像素视口下，页面与肉鸽容器无横向溢出，路线奖励和队伍面板可读。

本地 8000 服务更新前没有进行中的战斗，4 份存档已通过 SQLite 一致性备份保存至 `databases/fantasy-rogue-before-team-editor-2026-09-19T15-34-17-745Z.db`；重启后逐账号内容哈希一致。临时 QA 使用内存存档，不改玩家数据。本次浏览器验证仍为 HTTP 页面，未自动打开 `file://`。

### 幻想精英与太晶验证（2026-09-20）

独立内存 QA 服务（18001）配 HTTP 客户端（18081）实测：未解锁昵称的队伍页显示锁定、原生战斗无太晶选项，完成一场野怪战；通过服务端事件接口解锁的另一个 QA 昵称显示已解锁，在原生战斗勾选太晶后，战斗日志实际出现幸福蛋太晶化为一般属性。QA 使用明确设置的队伍和授权，不代表自然触发事件，也不是 `file://` 入口的浏览器测试。临时页面及服务已关闭。

8000 本地试玩已重新构建并重启，仍绑定 `127.0.0.1`、使用独立试玩数据库。更新前无进行中战斗；最终一致性备份为 `databases/fantasy-rogue-before-elite-tera-2026-09-20T10-42-01-787Z.db`，重启后四份账号存档逐项哈希一致。使用既有专用 QA 昵称通过真实 WebSocket 重连，确认冒险恢复、`teraUnlocked: false` 和队伍编辑字段正确。原 `testclient.html?~~localhost:8000#fantasyrogue` 页面刷新即可读取新规则。

### 战败、撤退与基础 PP 验证（2026-09-21）

服务端肉鸽六组回归共 59 项通过；最后补充对手伤害／异常重置断言后，真实房间与恢复专项 15 项再次通过。客户端肉鸽与 AI 入口回归 34 项通过。覆盖第三场全灭后复活继续第三场、撤退损耗、重新进入时对手恢复、急救固定 5000 且重复请求不重复扣款、连续挑战治疗锁、捕捉去重、旧 PP 与待学招式迁移、普通对战 PP 保持原规则。

服务端和客户端本地构建完成。新增及修改的肉鸽模块、专项测试通过定向 ESLint；`sim/pokemon.ts` 和旧 `fantasy-rogue.js` 测试文件仍有此前的格式问题，未扩大整理范围。全库 TypeScript 检查仍报原有六项 Mega 返回值类型错误，本次修改路径无错误；检查使用 `--noEmit --incremental false --ignoreDeprecations 5.0`。

独立内存 QA 服务（18001）配 HTTP 客户端（18081）实际操作：从人为设置的第 21 层第三场、1 HP 进入原生战斗；全灭后自动关闭战斗页并回到第三场，HP 为 0；支付 5000 后 HP 18/18、金币 6000→1000，仍进入第三场。再打一回合后撤退，场外保持 HP 16/18、跃起 PP 39/40。首页静态彩色按钮已实看，计算样式为渐变背景、无动画、原 16px 字体；420 像素视口下本层操作区无横向溢出。此为专项存档验证，不代表自然通关，也不是 `file://` 自动浏览器测试。

本地 8000 更新前无进行中肉鸽战斗。四份账号已一致性备份至 `databases/fantasy-rogue-before-encounter-recovery-2026-09-21T14-44-23-625Z.db`，重启后、读取新版存档迁移前的账号哈希全部一致。现有 QA 昵称通过真实 WebSocket 恢复原冒险，确认急救费用为 5000，所有保存招式上限均为基础 PP。仍绑定 `127.0.0.1:8000`，原 `testclient.html?~~localhost:8000#fantasyrogue` 页面强制刷新即可使用。

### 幻想精英名单替换验证（2026-09-25）

九层共 54 套配置与用户附件逐项比对通过，保留招式顺序、二招／三招配置、指定性别和全部 0 IV，不补招、不额外加道具。原生精英及试玩回归共 21 项通过，覆盖 54 个候选的入场、等级、特性、招式、个体值与太晶执行，以及候选保存和 v2／v3 存档兼容。服务端构建、修改文件的定向 ESLint 与差异格式检查通过；此为模拟器专项验证，不代表完整 AI 通关或难度评估。

确认无进行中的肉鸽战斗后，本地 `127.0.0.1:8000` 试玩服务已重启加载 v4。四份存档已备份至 `databases/fantasy-rogue-before-elite-pools-2026-09-25T14-26-54-763Z.db`，重启前后及既有 QA 昵称通过真实 WebSocket 重连后的账号哈希一致。只刷新查询不会更新保存的内容版本；下一次成功操作才升级版本，已选定对手保留，后续幻想精英层使用新池。

## 尚未完成的正式版内容

幻想碎片及专属技能池事件、构筑增益、支线奇遇、素材掉落、完整特殊进化仍待实现。已学招式的记忆与场外更换已接入。临时特殊战斗机制按原生每场规则，临时形态不写为永久形态。后续需完整客户端通关与经济／难度调参，再按用户授权合并、部署。
