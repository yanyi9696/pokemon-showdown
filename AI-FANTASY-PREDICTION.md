# AI 换人、Mega、特性与卡住修复

2026-09-25。

- 高难撤销后改选沿用同一请求编号，但增加决策修订号。原来只检查请求编号，会把新选择拒绝为旧任务；现在允许新修订重算，并在控制器和模拟器两端拒绝旧答案。
- 默认最多两个计算线程；单场取消、超时或崩溃只回收自己的线程。每个任务拥有独立思考预算，排队时间不扣除，撤销重试仍共享已用额度。线程退出、搜索超时或主线程准备异常都有合法动作兜底。
- 规则候选完成后立即发布原生评估结果，后续计算超时可保留已经考虑特性、形态和伤害的动作。
- 对手 Mega 使用已知或假设的配置在原生引擎中生成合法形态，比较进化前后的属性、特性和速度。普通增益形态的进化先验为 80%，平级变化为 50%，保留未进化可能；不是读取玩家是否按下 Mega。
- 双换先根据当前局面预测可能的换入成员，再对所有己方候选使用相同的分布。考虑免疫、承伤、入场伤害、下回合出手机会、损失的强化与无收益轮转；不会读取玩家的实际换人目标。保留有价值的双换进入后续推演。
- 强化招式用原生伤害比较核查实际增益，避免无视“纯朴”等特性而反复强化。队伍整体治疗同步清除公开记录的后排异常。
- UBUU 队伍按本次提供的六只配置替换；正式队伍均通过对应 FC 赛制校验。

人数是全服共享名额，默认 **2 场／每人 1 场，即最多同时 2 人**；所有阿塞萝拉条目和两档难度共用。详见 [本地配置与线程数](AI-FANTASY-LOCAL.md) 和 [时间预算](AI-FANTASY-TIME-BUDGET.md)。

回归包括真实工作线程撤销改选、并发隔离与相同工作预算、超时兜底、毒藻龙＋差不多娃娃与中毒／睡觉／治愈铃声、Mega 差不多娃娃的幽灵免疫、纯朴、飘浮、储水、厚脂肪、湿地减速，以及原有 AI 对战测试。

用户提供的 [回放 4635](https://play.psfantasy.xyz/replay/undefined-gen9fcuu-4635) 是 FC UU，共 26 回合并正常结束，包含 Mega 妙蛙花和微型湿地，不含毒藻龙或差不多娃娃。新增测试覆盖这些机制及另行构造的异常治疗场景；不能将它们称为对原始卡住对局的完整复现。对局回放不包含初始私人配置和真实请求，不把推测当成精确状态。

当前改动没有扩大普通／高难的信息权限，也没有增加搜索深度。策略是有限预算下的预测，线程池不会凭空增加机器 CPU；低配机器或提高对局上限后仍可能排队。部署服务端和客户端构建后需重启服务端、刷新客户端并新建挑战，已有 AI 管理器与对局不会热更新。
