# Fantasy AI 替身剧毒与坚盾剑怪策略

## 2026-09-09 开发约定

本节先于实现编写。用户提供的 [Smogon 配装介绍](https://www.smogon.com/dex/sv/pokemon/aegislash/national-dex-uu/) 强调：利用盾形态耐久在被动对手面前建立替身，再以剧毒、王者盾牌和剩饭逐步消耗；暗影球压制无法有效下毒的目标。介绍中的对手和伤害线来自原赛制，本地 Fantasy 机制必须以本仓库模拟器为准。

- 修正王者盾牌单招模拟的待行动队列，以及盾／剑形态切换后的能力值和承伤时机。较快攻击会先进入剑形态再承伤，较慢攻击先以盾形态承伤；王者盾牌变盾与保护成功率分别处理。
- 替身应在探针与整回合推演中实际承伤和阻挡异常，不能只是固定加分。考虑替身的 HP 成本、被打破、穿透、多段攻击、已存在替身及血量不足等情形。
- 普通／高难均基于允许的信息识别消耗机会：安全替身、有效剧毒、毒伤累积配合剩饭及保护、适时使用暗影球；不按训练家名称或固定回合脚本行动。
- 保护收益要扣除对手回复、强化、铺钉和异常招式的机会；王者盾牌不能阻挡变化招式。连续保护有失败风险，不能为了剩饭一直点盾；免毒、魔法镜、魔法防守、毒疗、挑衅等由原生探针与实际残伤判定。
- 与空间策略协调：空间窗口仍有机会成本，但回盾避死、有效毒杀和必要回复不能一概当成浪费回合。安全轮转与入场风险沿用现有模型。
- 不改用户正在编辑的队伍、规则效果或原有决策时间上限。新增有意义的原生战斗与策略回归，记录既有失败与本轮验证边界。

## 验证计划

采用用户的特耐替身剧毒配装及普通／Fantasy 两种坚盾剑怪，检查盾剑能力值、出手前后承伤、保护与连续保护、替身承伤和异常／穿透，以及面对被动对手、已中毒墙、免毒／反射目标、强化对手和空间局面的决策。再检查多回合搜索与信息隔离，并跑现有 AI 回归。

## 实现

- `hypotheses.ts`：己方请求可能保留基础形态名称，使用同一物种家族的公开形态变化日志识别当前盾／剑形态，避免误把幻觉伪装当成己方物种。己方使用训练家已知的性格、努力值和个体值；对手单招形态探针仅从允许的数据构造兼容配装假设。
- `matchup.ts`：为保护探针提供不执行的待行动占位，避免王者盾牌被空队列判为失败。保护概率由原生 `StallMove` 事件给出，变盾和保护失败分别形成结果分支；保留原生对变化招式、穿透和接触效果的处理。替身拥有 HP、会承伤和破裂，打替身不再被误判为完全无效攻击。
- `memory.ts`、`reconstruction.ts`：记录公开保护成功历史与连续使用计数；新替身初始 HP 可推知，被攻击后只保留公开信息能支持的 HP 区间。单招探针使用区间中值，整队推演使用不同区间假设，均不读取真实对手的隐藏计数。
- `policy.ts`：根据每个对手回应的实际优先度和速度，将攻击／保护／替身后的防守状态按先后手混合计入承伤，而非始终按出招前盾形态计算。保护下的保命计入与安全换人一致的关键成员收益。
- 消耗策略按“剧毒＋保护类招式”识别，兼容没有替身的剧毒王者盾牌配装。安全替身计入状态防护和后续可用性，剧毒计入后续净毒伤，保护计入毒伤、剩饭、回盾和连续失败风险，并扣除对手回复／强化机会。整回合推演继续计算铺钉和其他实际行动的后果。有效防守行动进入候选保留，原有空间回合机会成本及轮转判断继续参与比较。

## 2026-09-09 验证结果

- 新增 `test/server/fantasy-ai-subtoxic.js`，19 项定向检查通过，已加入 `npm run test:fantasy-ai`。包括真实验证过的 FC UU／FC Ubers UU 配装、普通和 Fantasy 形态、连续保护 1/3 概率、替身 HP、多段／声音／穿透、替身接剧毒接王者盾牌、免毒／魔法镜／魔法防守／毒疗、低血量、免费强化、空间毒杀、普通信息隔离及多回合搜索。
- 最终完整套件：172 项通过，包含此前的 22 项空间队回归；仍有 2 项本轮之前已存在的失败，分别是 `fantasy-ai-policy.js:174` 的重复换人历史断言，以及 `fantasy-ai-online.js:459` 将训练家列表首项假定为开发训练家的断言。
- 出手顺序用同一原生局面交叉核对：面对高速龙头地鼠，空间外先以盾形态承伤后攻击可以存活；空间内先攻击变剑后会被击倒，AI 的攻击评分相应下降。
- 构建和本轮文件的定向 ESLint、`git diff --check` 通过。队伍配置文件进入本轮时已有用户编辑，内容哈希保持不变；其既有尾随空白不计入本轮差异检查。
- 全仓 TypeScript 用 `--noEmit --incremental false --ignoreDeprecations 5.0` 检查，本轮文件无类型错误；仍有 6 项既有类型错误，位于其他世代规则、随机队伍和 `othermetas.ts`。

连续验证使用上述测试中的队伍，固定坚盾剑怪／呆呆王首发，对手包含呆呆王、河马兽、叉字蝠、雷公、炎帝、沙漠蜻蜓；双方每轮先独立决定再提交，公开日志按增量输入。普通和高难各运行 12 回合，单次搜索限制 36 个工作单位，种子均为 `gen5,0011001200130014`。

- 高难：26 次决策，无非法选择、探针失败或不支持世界诊断；单次最高约 1.25 秒。下毒后转入空间手的轮转计划。
- 普通：25 次决策，同样无非法选择、探针失败或不支持世界诊断；单次最高约 1.29 秒。出现剧毒、王者盾牌、替身与暗影球配合。
- 记录保存在 `logs/fantasy-ai-subtoxic-hard-1788961707871.json` 和 `logs/fantasy-ai-subtoxic-normal-1788961721918.json`。这两场是限定首发、回合数和搜索工作量的离线检查，不是胜率或完整默认搜索的性能基准。

本轮没有重启服务器或进行浏览器在线验收；已运行的 AI worker 需要重新创建或重启服务后加载新代码。
