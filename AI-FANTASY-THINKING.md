# Fantasy AI 不限思考时间

此文档记录上一阶段。用户随后因等待过长改为常规 10 秒、极少量关键决策 20 秒；当前配置与实现以 [AI-FANTASY-TIME-BUDGET.md](AI-FANTASY-TIME-BUDGET.md) 为准。

2026-09-08。用户明确取消 AI 思考时间限制。本轮继续先写开发文档，再修改实现；按既有要求不运行测试、离线对战或网页对战。

## 现有截断与目标

当前 `config/config.js`、示例配置和 `DEFAULT_LIMITS` 都使用 `decisionMs: 5000`；控制器扣除请求接收耗时，调度器在含排队时间的截止点终止工作线程，搜索器又将时间夹到五秒以内。此外，搜索默认最多计入 192 个模拟回合，浅层会提前跳到深层，深层只保留三个候选。只改一个配置项无法达到用户要求。

目标：默认不设 AI 决策墙钟截止时间，也不设累计模拟回合截断；完成有限搜索计划后落子。无限时间不等于无限循环或穷尽整局博弈，必须有清晰的算法结束条件。

## 实施约定

1. 用 `decisionMs: null` 表示不限时，贯穿正式本机配置、示例、管理器、控制器、调度器和工作线程消息；跨线程数据保持合法 JSON，不用超大数字或 `Infinity` 伪装预算。原有显式非负毫秒值继续支持，`0` 保持立即兜底的含义；不再有五秒上限。
2. 不限时请求不安装截止定时器。显式有限预算仍从收到请求开始计时，包含排队和线程启动；极长的显式预算分段设置定时器，避免 Node 定时器溢出后立即超时。
3. 搜索默认移除 192 回合工作量截断。保留开发工具显式设置有限 `maxRollouts` 的能力，不把该参数作为线上隐藏限额。
4. 搜索深度上限由 3 调整为 6，根部最多 8 个候选、对手最多 4 个候选、最多 4 种世界假设，每组合 2 个样本。不限预算时，每层完整遍历可支持的假设／对手回应／样本，保留根部候选到深层，取消为了抢时间而提前结束浅层或缩窄候选的逻辑。显式有限预算继续渐进发布已完成的公平比较。
5. 普通／高难共用相同计算方案。本节记录当时的信息权限；2026-09-10 已按用户授权扩展，见 [AI-FANTASY-INFORMATION.md](AI-FANTASY-INFORMATION.md)。高难可读取当前已合法提交的招式，后续假想回合仍由双方基于各自独立观察重新决策。
6. 源码核对确认 `RoomBattleTimer.start()` 已对所有 AI 挑战返回“不设每回合思考倒计时”，本轮沿用这项行为，无须修改计时器或开启玩家计时。玩家认输、销毁房间、新请求替代、断线清理依然取消工作线程任务。
7. 更新本机启动说明、当前策略说明和开发进度。历史记录保留，在旧限制旁指向本轮新约定。用户自行重启原 8000 服务并开新挑战加载修改。

## 边界与人工验收

- 不限时会增加每回合等待时间；本轮不运行对战，因此不承诺具体耗时或强度增幅。一个独立工作线程和并发名额约束仍保留。
- 首发与强制换人请求仍按现有规则入口处理；当前多回合搜索针对正常出招请求。有限候选、配置猜测、后续规则策略与复杂状态支持仍是搜索能力边界，六回合不是完整博弈解。
- 人工确认：复杂回合超过五秒仍继续思考，正常完成时不因默认 192 回合数回退；AI 挑战的计时按钮仍提示不设倒计时；认输／退出后任务被取消；其他真人对战计时不受影响。
- 本轮仅做构建、定向 ESLint、类型检查和差异空白检查，结果在开发进度中记录。
