# Fantasy AI 配招与本回合招式读取

## 2026-09-10 开发约定

用户授权扩大 AI 信息权限以增强对战能力。本轮先记录规则，再修改服务端、策略及客户端说明。

| 信息 | 普通 | 高难 |
|---|---|---|
| 自己的队伍、公开日志和队伍预览 | 可读取 | 可读取 |
| 对方全队每只宝可梦的初始四个招式（不足四招时按实际数量） | 可读取 | 可读取 |
| 对方全队初始道具、特性、太晶属性及精确能力值 | 仍通过公开信息和假设判断 | 保留现有初始快照 |
| 本回合对方已经合法提交、尚未执行的招式 | 不读取 | 可读取 |
| 当前精确 HP、隐藏 PP、随机数状态、未来随机结果 | 不新增读取 | 不新增读取 |

配招以独立 JSON 白名单传递，仅包含物种/形态及招式，不包含真实队伍槽位、昵称、道具、特性或能力值。排序不泄露预览阶段的出场顺序。公开的变身和配招变化仍按对战日志处理。

高难读取的是引擎已接受的选择，不是原始输入文字，也不预知玩家尚未操作时的意图。玩家选择换人时只能知道本回合没有选招式，不读取换入目标；选择 Z 招式或极巨招式时识别该招式。Mega、气场爆发和太晶化等其他尚未公开信息不额外读取。

## 实现范围

- 模拟器适配层：普通档也发送配招快照；高难通过私有消息发送当前选择状态及版本，信息不进入公开房间日志、回放或客户端请求。
- 控制器：高难普通行动回合等待对手合法提交后开始决策；队伍预览和必须补位不相互等待。玩家撤销或改选会取消旧任务；AI 提交时由模拟器再次核对版本，阻止陈旧结果执行。
- 时间预算：保持普通决策 10 秒、极少量关键决策 20 秒的现有配置。高难从获得可用选择信息时开始计时，等待玩家操作不消耗思考预算；同一请求重算沿用已开始的时间窗口。
- 对手模型：两档已知的配招用于所有队员的假设、威胁和换人评估，以及推演中的合法队伍重建；不再用猜测招式覆盖已知招式。
- 高难策略：规则评分和推演根回合均针对已提交招式计算；后续假想回合继续预测，不能把本回合信息当作未来每回合固定操作。普通档不会读取该字段。
- 离线评测：普通对手先提交后，高难再读取已接受的招式；两个高难互打时轮流先手提交，后提交的一方才能读取当前选择。不会构造双方同时预知对方尚未提交选择的局面。
- 客户端与房间提示：说明普通档知晓全队配招，高难档还知晓本回合所选招式，并保留高难已有的初始配置优势。

## 验证范围

- 普通配招白名单、拷贝隔离、对正常档不泄露当前选择。
- 已知配招进入所有队员的假设与世界重建，已知招式不被删减或替换。
- 高难等待、非法输入、撤销、改选、陈旧 worker 结果、模拟器最终版本核对、强制补位和回合切换。
- 已提交招式实际改变规则与推演回应；特殊招式只约束当前根回合。
- 使用真实房间/模拟器消息路径的定向自动化测试；相关构建、lint 和差异检查。网页完整对战强度仍需单独实战评估，不以静态检查代替胜率结论。

## 验证结果

- 服务端、客户端 `node build` 成功。客户端现有 PHP 新闻生成提示不影响构建完成。
- 新信息权限与线上撤销／版本校验的定向检查共 **11 项通过**，覆盖真实引擎、房间及模拟器传输。完整 `npm run test:fantasy-ai` 为 **203 项通过、2 项失败**；两项失败均已用修改前的 HEAD 源码和测试单独复现：重复换人惩罚断言，以及正式训练家 `acelora-ou` 与旧测试预期 `development-balanced` 不一致。本轮没有更改这两项既有行为或测试预期。完整回归组也通过了真实 worker 的高难出招与回合推进检查。
- 客户端 `test/fantasy-ai.test.js` 共 **15 项通过**，包括两档新说明。
- 改动代码的定向 ESLint（`--max-warnings 0`）及 `git diff --check` 通过。
- 全仓类型检查使用 `--noEmit --ignoreDeprecations 5.0 --incremental false`，仍为此前记录的六处既有错误：`data/mods/gen8/rulesets.ts:98,126`、`data/mods/gen9ssb/scripts.ts:963`、`data/random-battles/gen7/teams.ts:1607`、`server/chat-plugins/othermetas.ts:130,131`；本轮没有新增类型诊断。

未重启现有服务，未运行完整浏览器对战或强度评测。重启服务端、刷新客户端并新建挑战后加载更新；上述检查证明信息和决策路径生效，不代表已经达到特定胜率。当前 FC 候选枚举对极巨化的原有限制保持不变，本轮的选择快照识别不会扩展赛制规则。
