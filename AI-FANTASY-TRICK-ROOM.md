# Fantasy AI 戏法空间队策略

## 2026-09-09 开发约定

本节先于实现编写。用户反馈阿塞萝拉 FC UU 队伍只进行普通轮转和消耗，未主动为低速输出手创造空间环境。本轮补充通用空间队逻辑，不更改用户配装、不绑定训练家名称或固定回合。

- 从存活成员的合法配招、速度、攻击手段和道具识别空间使用者与受益输出手；与可见信息构造的对手假设比较，不能把所有低速成员或所有携带空间的队伍一律视为受益。
- 安全窗口优先考虑开启戏法空间；当前成员无法开空间时，给能够安全入场并完成开空间的成员计入后续收益。即时击杀、避免被击倒、挑衅／封印等出招限制仍参与判断。
- 空间开启后，利用瞬间移动等轮转及免费补位让低速高输出成员上场；仍计算入场钉子、承伤、实际打击面和剩余回合，避免为了轮转耗尽空间。
- 有利空间内优先兑现输出，降低无必要的保护、满血回复、反复强化等耗回合行为；空间结束后根据存活受益者重新争取开空间机会。不能在己方受益时把再次使用戏法空间误当成刷新持续时间；对方更受益时允许反开解除。
- 原生单招探针记录戏法空间实际开启／解除。公开记忆记录持续时间，识别 Persistent 的公开延长标记；假想世界保留剩余回合。实际规则由本地 `data/moves.ts`、`data/mods/gen9fantasy/abilities.ts` 和模拟器决定。
- 规则候选保留有效空间行动，短期／多回合搜索计入空间的队伍收益与剩余输出窗口；普通与高难沿用各自的信息权限，保持现有 10 秒／少量 20 秒时间预算。

## 验证范围

使用用户的真实 UU 配装构造定向局面，检查开空间、换入空间手、空间内轮转与输出、空间消失后重开、低血量／挑衅／先制招式／对方更慢等反例。检查普通与 Persistent 的剩余回合及推演一致性、普通档信息隔离；运行相关回归、构建与定向静态检查，结果在完成后补充。

## 实现

- `server/fantasy-ai/trick-room.ts`：按存活输出手、有效速度、攻击手段和允许读取的对手假设计算队伍空间收益。考虑能力等级、麻痹、围巾、减速道具、顺风和常见天气加速；输出角色识别计入粗骨头等道具，先制攻击不因空间获得跨优先度的收益。这里是轻量角色估值，具体伤害、出手顺序和存活仍由原生探针及整回合模拟校验。
- `policy.ts`：为适合的空间手首发、安全开空间及换入空间手计入收益；空间内优先使用能够取得进展的攻击，并在有更强后排输出手时考虑瞬间移动。考虑剩余回合和入场伤害，保留紧急回复／安全换人及立即击杀的价值。空间行动进入候选保留逻辑，避免搜索缩窄时遗漏。
- `matchup.ts`：记录空间实际开启／解除，使用原生 `DisableMove` 处理挑衅、封印等确定性限制；不会把被禁止的空间招式算作成功。睡眠、麻痹的行动概率仍由原有路径统一计算。
- `memory.ts`、`reconstruction.ts`：从公开的 `[persistent]` 标记区分 5／7 回合，推演继承剩余回合；补全公开封印状态的施放者。
- `rollout.ts`：把空间的剩余队伍收益计入局面价值。场上成员用模拟器的实际速度；后排按入场后的角色估值，避免因为模拟器在后排暂停道具／特性而漏算粗骨头等收益。

## 2026-09-09 验证结果

- `node build` 通过；修改涉及的 7 个 TS／JS 文件定向 ESLint 与 `git diff --check` 通过。
- 新增 `test/server/fantasy-ai-trick-room.js` 共 22 项定向检查，全部通过，并已加入 `npm run test:fantasy-ai`。
- 完整 Fantasy AI 套件：153 项通过，2 项仍为本轮之前已出现的失败：`fantasy-ai-policy.js:174` 的重复换人历史断言，以及 `fantasy-ai-online.js:459` 把真实训练家列表首项假定为开发训练家的断言。
- TypeScript 使用与现有编译器兼容的 `--ignoreDeprecations 5.0 --incremental false` 检查：本轮文件无类型错误；全仓仍有 6 项既有错误，位于 `data/mods/gen8/rulesets.ts`、`data/mods/gen9ssb/scripts.ts`、`data/random-battles/gen7/teams.ts`、`server/chat-plugins/othermetas.ts`。

另通过 `runOfflineBattle` 使用配置中的 `acelora-uu`，对阵携带正常攻击、回复及轮转招式的高速队伍（Mew、Crobat、Raikou、Entei、Starmie、Choice Scarf Flygon）。双方使用相同难度；对战和决策种子均为 `gen5,0011001200130014`，回合上限 24。

| 策略／信息难度 | 已跑回合 | 阿塞萝拉开空间回合 | 结果边界 |
| --- | --- | --- | --- |
| 规则／高难 | 13 | 1、8 | 完整结束 |
| 规则／普通 | 18 | 1、11 | 完整结束 |
| 多回合搜索／高难 | 24 | 1、8、18 | 达到检查回合上限 |
| 多回合搜索／普通 | 17 | 1、6、11 | 完整结束 |

四场均无非法选择和探针失败。高难搜索实际出现第 1 回合黑夜魔灵开空间、第 2 回合瞬间移动接泥偶巨人、第 3～5 回合连续攻击的过程，之后由魔灵珊瑚重新开空间并换入嘎啦嘎啦。两场搜索均没有搜索回退或不支持世界的诊断；单次决策最高约 1.18／2.02 秒。此次每次搜索限制为 36 个推演工作单位，`budgetStops` 来自该显式上限；这不是完整默认搜索的时间或强度基准。

本轮验证为原生模拟器离线对战，未进行浏览器在线验收。已在运行的 AI worker 需要重启服务或重新创建后载入新代码；本轮没有重启运行中的服务器。
