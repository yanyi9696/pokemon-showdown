# Fantasy AI：优先 Mega 与难度权限复核

2026-09-18。用户希望通常尽早 Mega，只在进化明显不利或种族值没有净提升的形态上保留更多取舍。本轮先记录开发约定，再修改实现；按用户要求不运行测试或对战模拟。

## 行为约定

- 普通和高难共用 Mega 策略。常规 Mega 获得持续形态提升的偏好，不再因为“消耗一次资源”在规则评分、推演终局中重复扣分；伤害、生存、速度、特性和有效行动仍按当前 FC Dex 的原生机制判断。
- 使用实际进化前后形态的种族值总和判断提升，不假设所有 Mega 都增加 100。总和没有净提升时不加自动进化奖励，例如两种自制武道熊师 Mega 保留按属性、招式、特性与局面选择的行为。
- 面对对方存活队伍时，如果进化新增的弱点覆盖至少半队且通常至少两只，先用已获准的全队配招筛选，再用少量原生伤害探针确认明显承伤恶化。确认后压低进化的长期收益；剩余仅一只时按该只判断。不因一个普通弱点就放弃 Mega，也不把静态属性倍数当作真实伤害。
- 当前行动如果由原本较安全变成明显容易被击倒，且不能靠先手击倒对方避开，则取消常规 Mega 奖励。仍允许搜索在明确的交换或胜利收益下选择 Mega，不设置永久禁止标记。
- 根部候选在评分接近时保留 Mega 与同招式的不进化版本，防止进化候选在有限搜索开始前被挤掉。后续推演沿用同一规则；有效换人、急救与终结对手的候选继续参与比较。
- 不强制执行已被引擎禁止或已被拒绝的 Mega；无效攻击过滤、四类机制规则与 Z／气场互斥不变。工作线程未完成任何分析时，主线程仍使用原有静态合法兜底，不为了强行进化运行额外模拟。

## 实现与耗时

新增 `server/fantasy-ai/mega.ts` 作为形态偏好入口，复用已建立的合法观察、全队假设与单招探针；同一形态的全队判断每次决策缓存一次，只有静态筛选发现严重退化才增加最多两名代表对手的进化前后探针。调整 `policy.ts` 的资源成本、候选选择和即时危险判断，移除 `rollout.ts` 对已 Mega 的固定扣分。

保持常规 10 秒、少量关键决策 20 秒的预算、次数与间隔，不扩大搜索深度和线程数。偏好必须进入 `strategic`，避免短期推演再次无理由推迟 Mega；所有加减分都可被实际生存、击倒及胜负收益覆盖。

## 两档信息权限复核

从 `server/room-battle.ts` 的私有消息采集、`initial-snapshot.ts` 白名单、`information.ts` 难度隔离，追踪到 `hypotheses.ts`、`policy.ts` 和 `rollout.ts` 的实际使用：

| 信息 | 普通 | 高难 |
|---|---|---|
| 自己的队伍、公开队伍预览和对战日志 | 可用 | 可用 |
| 挑战者全队每只的初始配招 | 可用 | 可用 |
| 挑战者全队初始道具和精确六项能力值 | 不直接读取，按公开信息推测 | 可用 |
| 挑战者本回合已合法提交、尚未执行的招式 | 不读取 | 可用，仅约束当前回合 |
| 挑战者全队初始特性、太晶属性、等级和形态 | 仅公开或推测内容 | 原有完整配置快照中可用 |
| 当前精确 HP、剩余 PP、未公开动态状态、随机结果 | 不直接读取 | 不直接读取 |

普通的额外私有信息只有物种对应的初始配招。高难比用户本次列举的项目还保留原设计中的初始特性、太晶属性等字段，必须在交付说明中如实指出。本轮不扩大也不缩小权限。

高难读取的是玩家已经提交且引擎接受的招式，不预知尚未输入的操作；选择换人时不读取目标，未公开的 Mega／太晶／气场选择也不读取。开局快照不是实时透视，之后按公开日志更新。撤销／改选沿用版本核对，旧结果不得提交。

## 静态检查与人工验收

本轮执行构建、定向 ESLint、全仓类型检查与差异检查，不运行自动化测试、离线模拟、网页对战或重启服务。检查结果写入开发进度。

用户之后可重点观察：普通增益 Mega 能否在安全出招、回复或撒钉时及时使用；双方当前伤害接近时是否仍反复推迟；进化丢失关键抗性且被多名存活对手克制时能否保留原形；两种自制武道熊师 Mega 是否根据具体局面取舍；高难是否等待玩家提交、改选后是否重新响应。静态检查不代表这些行为已经通过实战验收。
