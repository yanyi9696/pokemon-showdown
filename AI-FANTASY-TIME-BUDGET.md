# Fantasy AI：常规 10 秒，少量关键决策 20 秒

2026-09-08。用户实战反馈不限时过慢，要求常规思考控制在 10 秒内，极少量关键回合允许 20 秒。本轮先编写此文档，再修改代码；沿用用户自行测试的要求，不运行测试或对战模拟。

## 预算约定

- 常规决策上限 `decisionMs: 10000`，关键决策上限 `criticalDecisionMs: 20000`；20 秒是从收到该次决策请求起的总上限，包含请求准备、排队、线程启动与搜索，不能变成 10+20 秒。
- 关键额度默认每局最多 2 次，从第 10 回合起才能使用，前后两次至少间隔 10 个完整回合。额度在分配时计入，即使提前完成也不退还，保证长思考不会频繁发生。
- 只有正常出招请求、存在至少两种合法选择且符合下列之一时允许关键额度：双方剩余不超过三只，己方当前成员不高于 65% HP 且存在可用特殊机制；双方剩余不超过两只并进入相互收割的低血量区间；己方仅余不超过三只，面对公开的高进攻强化或攻速强化威胁。
- 所有判断只读自身请求与公开日志，使用静态 Dex 信息，不在服务端主线程运行原生伤害探针或搜索。首发、强制换人、只有一个合法选择以及普通异常／普通换人本身均不自动获得 20 秒。
- 同一回合、相同阶段与己方在场成员的非法行动重试沿用原截止时间及额度，不因新的请求编号或不可用招式提示重新获得 10／20 秒。折返后的独立补位请求属于新的普通决策窗口。
- 先完成便先出招，不故意等满预算；到截止点返回已完成的最佳结果并终止仍在运行的工作线程。没有搜索结果时使用规则结果，工作线程尚未给出结果时保留静态合法兜底。

## 实现范围

1. 更新本机实际配置、示例及共享默认值；管理器将常规和关键上限分别传给控制器与调度器，避免关键 20 秒仍被调度器夹回 10 秒。
2. 增加独立时间管理模块，处理关键情景、次数／间隔限制及重试不刷新截止点。增加已分配的关键额度和原因统计，便于用户之后观察。
3. 调度器对单个请求的关键标记选择对应上限；总时间由主线程硬截止，工作线程只拿剩余预算，不能在开始执行时重新计时。额度选择由可信控制器完成。
4. 恢复有限预算下的渐进搜索：常规最多六个根候选、深层三个、最多四回合；关键最多八个根候选、深层四个、最多六回合。仅发布已完成的公平比较；接近截止时停止启动预计来不及完成的新一轮，给返回结果留出少量时间。最多四个对手候选、四个世界假设与每组合两个样本沿用。
5. 不恢复 192 个模拟回合的默认固定截断，时间是当前主要预算；显式开发工作量上限及手动 `null` 不限时配置仍保留，但本机和默认配置改为 10／20 秒。
6. 原有 AI 对局不设玩家回合倒计时、认输取消任务、断线清理、四机制规则及信息权限保持原来的行为。更新当前文档并注明不限时文档属于上一阶段。

## 检查与边界

本轮只做构建、定向 ESLint、配置语法、全仓类型和差异空白检查。用户人工确认：常规请求约在 10 秒内结束、每局最多两次关键额度且不连续、关键请求总计不超过约 20 秒、非法重试不刷新额度、认输后思考终止。

这里的硬截止依赖 Node 主线程定时器，极端系统卡顿时发送动作仍可能有调度延迟，不能承诺操作系统级实时精度。关键识别是保守的公开局面启发式，不等同于已经证明的决胜点；实际耗时、搜索深度与策略质量由用户实战检查。代码与配置更新后需要重启原 8000 服务并新建挑战。
