# Fantasy AI 开发进度

需求依据：[AI-FANTASY-DESIGN.md](AI-FANTASY-DESIGN.md)。本文记录实际实现状态及用户确认的需求变更。本机运行方式见 [AI-FANTASY-LOCAL.md](AI-FANTASY-LOCAL.md)，编辑 AI 队伍见 [AI-FANTASY-TEAMS.md](AI-FANTASY-TEAMS.md)。

开发分支：`codex/fantasy-ai`，起点为 `5621e26f7`。不在 `master` 上开发。

## 第一阶段：推演前置验证与服务端基础

### 已实现

- 将 17 个 FC 赛制重复的显示同步逻辑提取到 `data/mods/gen9fantasy/visuals.ts`。不再把 `fantasySync` 闭包存入 `Pokemon.m`，JSON 恢复后可继续处理形态和幻觉显示。赛制规则、队伍限制、伤害计算保持原样。
- 训练家注册表：校验稳定标识、名称、风格、关键成员、资源偏好、固定队伍和实际 FC 合法性；错误配置单独停用，重复标识全部停用，并提供管理端诊断。
- 对训练家与玩家队伍应用相同的六只、每只一至四招和赛制合法性要求；排除随机队伍、九选六、六选三、双打、多人、自定义超大队伍及附加规则。
- 正式配置 `config/fantasy-ai-trainers.ts` 初始为空。开发样例单独放在 `config/fantasy-ai-trainers.example.json`，带有明确的开发标记。注册表默认关闭，测试中必须显式启用；开发样例还需要单独允许。
- 普通档只接收自身请求与公共协议；高难档额外接收队伍预览时采集的初始配置及精确能力值副本。快照按配置排序，不保留队伍位置、昵称或出场身份关联。动态状态仅通过公开事件更新后续模型，初始快照本身不变。
- 信息视图剔除协议中的私有分支、聊天、调试、请求和非预期字段；所有输出均为独立数据副本，策略接口不接收真实 `Battle`、对手请求、行动队列或随机源。
- 单打行动候选枚举：720 种完整队伍预览顺序、普通招式、换人、强制换人、复生祈祷候选、Mega / Mega X / Mega Y 协议标记、Z 招式、太晶化和通过 `ultra` 协议执行的气场爆发。各特殊操作为独立候选，不拼接互斥标记。
- 定义普通、高难共用的初始预算常量：一个工作进程、两场全服并发、每人一场、五秒决策预算、十分钟断线保留和最多 192 次推演的候选上限。

第一阶段完成时，这些模块尚未接入在线对战房间，预算仅为常量定义。第三阶段增加独立决策调度器，第四阶段将其接入服务端常规房间。

### 验证方式

在服务端仓库执行：

```powershell
npm run test:fantasy-ai
```

该命令调用现有构建与测试初始化，运行训练家配置、信息边界、行动枚举和 Fantasy 状态恢复测试。恢复测试比较完整标准化状态、日志和请求，并覆盖 Mega、幻觉解除、变身后换人、气场爆发及回合中途急速折返补位；单独检查推演分支不会修改原对局。

本阶段新增 52 项测试。执行 `npm run test:fantasy-ai -- test/sim/misc/state.js`，连同原有两项序列化测试共 54 项全部通过。另已对照 `master` 核实：除提取这 17 组显示同步处理函数外，`config/formats.ts` 的赛制规则与其他事件处理逻辑均未改变。

行动候选由真实引擎验证。隐蔽的踩影、禁用等机制仍可能使基于请求生成的候选在提交时被拒绝，后续控制器必须处理引擎返回的新请求，不得靠读取对手内部状态提前排除这些候选。

新增模块的代码检查：

```powershell
node node_modules/eslint/bin/eslint.js --max-warnings 0 server/fantasy-ai/*.ts config/fantasy-ai-trainers.ts data/mods/gen9fantasy/visuals.ts test/server/fantasy-ai.js test/sim/misc/fantasy-state.js
```

本机已有全仓检查限制：

- `package.json` 声明 TypeScript `^6.0.2`，实际安装版本为 `5.7.3`；原有 `ignoreDeprecations: "6.0"` 使 `npm run tsc` 直接报 TS5103。
- 临时通过命令行指定 `--ignoreDeprecations 5.0` 后，全仓仍有六处类型错误，位于 `data/mods/gen8/rulesets.ts`、`data/mods/gen9ssb/scripts.ts`、`data/random-battles/gen7/teams.ts` 和 `server/chat-plugins/othermetas.ts`，涉及既有 Mega 道具的字符串或数组类型。
- 本阶段不修改这些已有依赖、配置或其他赛制的类型问题。不能将本阶段功能测试通过表述为全仓类型检查通过。

## 第二阶段：规则决策与离线对局

### 已实现

- `memory.ts` 从过滤后的公共协议重建回合、血量区间、异常状态、招式使用记录、公开道具与特性、强化、场地、天气和特殊资源使用情况。记录同基础优先度的出招顺序，但不将其当成排除速度相同或优先度修正后的确定速度关系。
- 公开信息无法排除幻觉时，保持不同出场记录的身份歧义，不使用内部槽位关联。只有公开身份足够明确时，才跨换人保留招式与道具线索；临时强化与特性变化不会直接沿用。被挥指、梦话等调用的招式不当成新增可选招式或独立 PP 消耗。
- `hypotheses.ts` 用本地 Fantasy / Gen 9 配招样例和赛制允许的学习面生成普通档配置先验；高难档用无序初始快照替换相应先验，叠加后续公开的道具、特性和形态变化。两档均保留可行的幻觉身份假设，最多四种，并使用同一套策略和预算常量。
- `matchup.ts` 从数据新建隔离的假想双方，使用 Fantasy 引擎执行单招估算，统计伤害、击杀、恢复、反伤、强化、状态、场地和速度收益。原生引擎处理属性免疫、特性吸收、反射、天气命中率及特殊操作；独立固定种子用于两次采样，真实对局及其随机源不参与估算。
- `policy.ts` 加入均衡、强攻、稳健三种风格的规则评分；包含可靠收割、命中风险、先制、恢复、强化、场地、异常状态、换人风险、关键成员和资源偏好。预览使用首发评分；强制换人按候补对位评分；近期重复换人和收益不足的特殊资源消耗受到惩罚。输出最多六个候选，仅在接近的评分之间使用显式决策种子选择。
- `offline.ts` 驱动经过赛制校验的完整六对六本地对局。双方策略只收到各自的信息视图，先完成双方观察与决策再提交行动；处理请求更新、已提交选择和被拒绝的候选，设置回合与请求次数上限。结果区分正常结束与测试限额，并统计耗时、非法行动、估算异常及省略临时状态的决策数。
- `tools/fantasy-ai-smoke.js` 显式启用开发样例运行普通对高难联调，不依赖在线房间或正式训练家配置。

### 验证方式

```powershell
npm run test:fantasy-ai -- test/sim/misc/state.js
npm run smoke:fantasy-ai
node node_modules/eslint/bin/eslint.js --max-warnings 0 server/fantasy-ai/*.ts test/server/fantasy-ai-policy.js tools/fantasy-ai-smoke.js
node node_modules/typescript/bin/tsc --pretty false --ignoreDeprecations 5.0
```

行为测试覆盖收割、已知免疫、低血量恢复、安全强化及强化上限、危险对位换人、重复换人惩罚、气场爆发、反射场地和天气 / Z 招式命中率。信息边界测试直接改变真实对手血量、PP、道具、待执行行动及随机源，验证在可见信息相同时两档的决策均保持不变，观察副本及真实对局不被估算修改。离线测试覆盖完整六对六结束和测试回合上限。

2026-09-06 验证结果：本阶段新增 18 项测试，连同前阶段及原有序列化测试共 72 项全部通过；新增代码通过 ESLint 检查。全仓类型检查只报告前述六处既有错误。

固定开发样例的普通对高难联调在 111 回合正常结束，高难方 `AI P2` 获胜；245 次决策中非法行动和估算异常均为零。本次本机运行平均规则决策耗时约 112.7 毫秒，最长约 310.0 毫秒。这是一场可复现功能样例，不构成强度或满载性能验收。

### 第二阶段完成时的边界

- 这是规则候选评分与离线功能联调。单招估算尚未实现双方同时出招的一整个回合，不包含完整候补队伍、追加换人、回合末结算或所有临时状态与计数器；省略的临时状态会在诊断中标记。单招估算结果不能代表完整推演收益。
- 普通档先验只是推测，招式来自合法学习面并过滤直接禁招，尚未验证每组假设配招的完整组合合法性。形态变化后的能力值属于近似投影，变身等复杂情形仍需后续假想局面构造完善。
- 同步离线驱动尚无工作进程、排队调度或五秒超时保障。一次联调的胜负与耗时仅用于功能验证，不能代表强度门槛、满载性能或线上可用性。
- 全仓类型检查仍受第一阶段记录的六处既有错误限制。

## 第三阶段：整队假想局面、整回合搜索与工作线程

### 已实现

- `sets.ts`、`world.ts` 使用固定训练家配置、自身请求、公共日志和获准的无序初始快照组装完整六对六假想队伍。假想对手的顺序由模型生成，不能读取真实位置。高难档初始能力值对应的性格、个体值和努力值仅推导为兼容配置，不声称还原真实隐藏配置。每组初始配招及整队通过实际赛制校验，包含组合禁用规则；普通档只可调整未公开的假设，不能删改已知招式来绕过校验。
- `memory.ts` 增加公开状态来源、计时起点、睡眠行动、剧毒结算次数和临时效果数据；处理换场交换场地效果、永久形态身份及太晶状态延续。挣扎不计入已知可选招式；换人后清除上一轮在场的最后招式及行动计数，防止错误保留讲究道具锁定或误判击掌奇袭。
- `reconstruction.ts` 从上述数据新建独立 Fantasy 对局，恢复双方整队、当前场上成员、自身精确状态与当前招式 PP、对手血量区间假设、公开强化、异常状态、已支持的临时状态、天气、场地和特殊资源。自身行动编号与真实请求逐项核对；不复制真实对局、待执行选择、行动队列或随机源。
- `rollout.ts` 先固定假想对手的候选及概率，再让自身最多六个候选面对相同情景。由原生引擎执行双方行动、换人触发、入场伤害、回合末结算，以及急速折返、倒下补位和复生祈祷等追加请求，直到下一常规请求或结束。额外选择仅用该假想对局的独立信息视图和规则策略完成。
- 两档共用最多四个对手候选、四种假设、每种组合两个独立随机样本和 192 次推演上限。按假设概率计算期望与风险惩罚；只有所有自身候选都完成同一情景的比较，才更新最佳结果。遇到预算耗尽或异常时舍弃未完成的这一轮比较，保留此前完成的结果或规则兜底。评分计入恢复 PP 消耗和可用进攻强化，降低耗尽进攻 PP 后留场的价值。
- `scheduler.ts`、`worker.ts` 增加一个独立 Node 工作线程及主线程截止计时。默认最多注册两场对局，单请求五秒包含排队、复制和线程启动；超时优先返回已完成的搜索或规则候选，否则使用自身请求生成的行动。按房间、实例、递增请求编号和任务令牌校验结果，取消被新请求替代的任务，忽略迟到结果；线程退出时返回兜底并为后续任务重启。更换工作线程前等待旧线程退出，避免推演线程叠加。
- 离线驱动增加搜索模式及推演数、搜索回退、预算停止和不支持情形的统计。可选开发日志只写观战者可见的协议分支，验证不包含请求、私有血量或高难快照。

### 验证方式

```powershell
npm run test:fantasy-ai -- test/sim/misc/state.js
npm run smoke:fantasy-ai:rollout
node node_modules/eslint/bin/eslint.js --max-warnings 0 server/fantasy-ai/*.ts config/fantasy-ai-trainers.ts data/mods/gen9fantasy/visuals.ts test/server/fantasy-ai.js test/server/fantasy-ai-policy.js test/server/fantasy-ai-rollout.js test/sim/misc/fantasy-state.js tools/fantasy-ai-smoke.js
node node_modules/typescript/bin/tsc --pretty false --ignoreDeprecations 5.0
```

构建后需要公开协议追踪时运行 `node tools/fantasy-ai-smoke.js --rollout --trace`；日志保存在已忽略的 `logs/` 目录，不作为配置或公开回放导出。

新增测试覆盖普通 / 高难与双方位置、整队合法性、能力值兼容推导、幻觉假设、讲究锁定解除、耗尽 PP、场地时限、换场与睡觉来源、完整换人链、倒下补位、复生祈祷、Mega / 气场爆发及假想对局的 JSON 恢复一致性。搜索行为测试覆盖守住配合残余伤害击杀、安全强化、进攻 PP 耗尽后的换人、完整离线结束、未授权真实状态改变时的可复现性和公开日志隔离。实际工作线程测试覆盖主线程响应、排队超时、保留已有候选、实例替换、迟到结果、线程退出重启和容量释放。

2026-09-07 最终验证：本阶段新增 31 项测试，连同前阶段及原有序列化测试共 103 项全部通过；上述范围内的 ESLint 检查通过。全仓类型检查仍只报告第一阶段记录的六处既有错误，未报告本阶段模块的类型错误。

最终固定开发样例的普通对高难联调在 80 回合正常结束，高难方 `AI P2` 获胜。173 次决策共执行 21,168 次整回合推演，非法行动、单招估算异常、搜索回退、预算停止和不支持情形计数均为零。本次本机同步驱动平均决策耗时约 635.7 毫秒，最长约 2791.4 毫秒；该时间不代表在线排队或满载性能。原始公开协议追踪位于 `logs/fantasy-ai-public-1788712124405.log`，该开发日志不纳入提交。

### 第三阶段完成时的边界

- 已建立整回合执行链，但尚未覆盖 Fantasy 全部运行状态。变身、无法确定的倒下幻觉身份、不支持的公开临时状态或行动映射会明确回退。愿望、延迟攻击等槽位效果及部分自定义特性计数器仍需后续补齐和一致性测试，当前不能宣称任意中盘局面均已完整还原。
- 对手血量、剩余 PP、隐藏计时和形态变化后的能力值仍由获准线索推测；自身候补的 PP 也暂按已知公开使用记录估计。假想世界带有 `hypothesized-hidden-counters` 诊断，不能把本次样例中零搜索回退解释为没有假设误差。
- 离线完整对战使用同步驱动，只进行协作式预算检查；硬截止由独立调度器的主线程计时和工作线程终止保障，二者分别测试。调度器尚未接入真实在线房间，未实现按玩家限额、连接状态或十分钟清理。
- 一场固定样例及低预算功能对战均不构成强度或满载性能验收。正式配置继续为空，功能保持未向玩家开放；全仓类型检查仍受六处既有错误限制。

## 第四阶段：服务端挑战与对局生命周期

### 已实现

- `manager.ts` 加载正式训练家模块及 `Config.fantasyai`，复用现有异步队伍验证、昵称检查、封禁和创建频率限制，再应用 AI 的六只与赛制结构校验。异步验证前预留全服和玩家名额，验证失败、身份变化、创建失败或对局结束时释放；默认全服两场、每名玩家一场。正式开关默认关闭、正式训练家仍为空，开发样例不会自动导入。
- `RoomBattleAIPlayer` 明确标记占用的 AI 席位，使用固定训练家队伍和 `AI · 训练家名称` 显示名，不创建虚拟用户账号或连接。AI 不作为断线玩家处理，也不能通过加入游戏、替换玩家等操作接管该席位。人类玩家沿用现有请求、操作、观战和房间协议；认证改名后保持席位和并发限制。
- `controller.ts` 将过滤后的公开协议及 AI 自身请求交给第三阶段调度器。模拟器通过专用内部消息在预览时仅采集一次高难快照，普通档不采集；快照不进入房间日志或输入日志。另一条内部消息标记本批请求和公共更新均已发送，避免 AI 在看见本回合公开局面之前决策。
- 每次提交绑定房间、实例及请求编号；收到新请求时取消旧任务。引擎返回 `Invalid choice` 或 `Unavailable choice` 后，记录被拒候选并基于更新后的请求重新选择。工作线程退出、排队超时或决策异常仍可提交合法兜底；房间结束或销毁后丢弃迟到结果。
- AI 挑战为不计天梯的普通挑战。思考倒计时始终关闭，包括服务器强制计时设置；已连接的玩家不会被房间空闲清理器提前结束。断线或离开房间后默认保留十分钟，不替玩家自动选择；重连恢复自身请求并取消清理。到期即使还有观战者也关闭房间、撤销任务并释放名额。主动认输及离开游戏按正常认输结束。
- 增加训练家列表、挑战、重赛与管理员诊断命令；重赛校验原对局玩家身份并使用当前提交的队伍。`/cmd fantasyai` 提供客户端所需的训练家摘要、难度及本人尚未结束的 AI 房间，不暴露配置、快照或管理员诊断。
- 完成对局记录信息档位、训练家标识、决策次数与耗时、推演数、超时、规则回退、非法行动、工作线程错误和结束原因。管理员可查看最多一百场近期统计及注册表诊断；公共房间只显示训练家、难度和高难信息权限说明。

### 接口与配置

开发联调命令：

```text
/fantasyai list
/fantasyai challenge <trainer-id>, normal
/fantasyai challenge <trainer-id>, hard
/fantasyai rematch
/fantasyai status
/cmd fantasyai
```

挑战使用现有 `/utm <packed-team>` 提交的队伍；图形化队伍选择入口尚未接入。`rematch` 在本人已结束的 AI 房间中使用；`status` 要求管理员的 `lock` 权限。`/cmd fantasyai` 的返回协议为 `|queryresponse|fantasyai|<JSON>`，字段包括 `enabled`、`trainers`、`difficulties`、`activeBattles`。

配置示例已加入 `config/config-example.js`：

```javascript
exports.fantasyai = {
    enabled: false,
    allowDevelopmentTrainers: false,
    maxBattles: 2,
    maxBattlesPerPlayer: 1,
    decisionMs: 5000,
    disconnectMs: 10 * 60 * 1000,
};
```

训练家内容和配置通过重启服务加载。本阶段未修改本机的实际 `config/config.js`，也未开启面向玩家的正式功能。两档始终共用相同配置；决策预算不能超过五秒。工作线程数量仍固定为一个，全服对局数可配置为 1 至 16；调大名额不代表已通过相应负载测试。

### 验证结果

```powershell
npm run test:fantasy-ai -- test/sim/misc/state.js test/server/room-battle.js test/server/rooms.js test/server/users.js
node node_modules/eslint/bin/eslint.js --max-warnings 0 server/fantasy-ai/controller.ts server/fantasy-ai/manager.ts server/fantasy-ai/scheduler.ts server/room-battle.ts server/rooms.ts server/chat-plugins/fantasy-ai.ts config/config-example.js test/server/fantasy-ai-online.js
node node_modules/typescript/bin/tsc --pretty false --ignoreDeprecations 5.0
```

2026-09-07：本阶段新增 20 项测试，AI 模块与原有序列化测试合计 123 项；加上现有房间、模拟器抽象和用户连接回归测试，共 154 项全部通过。上述新增及改动范围 ESLint 通过；类型检查仍只报告此前六处既有错误。

测试实际创建普通和高难对战房间，完成一场六对六房间的全部请求，并分别验证真实搜索工作线程、原生非法 / 暂不可用行动重试、并发预留、非法队伍、认证改名、验证中身份变化、认输重赛、超时兜底、线程退出恢复、真实断线重连、观战者存在时的清理及迟到结果隔离。另使用真实模拟器子进程验证高难快照与更新边界消息的跨进程传输。十分钟清理的逻辑测试使用缩短的可配置时长，并单独确认生产默认值为 600000 毫秒。

### 第四阶段完成时的边界

- 服务端命令和客户端查询协议已实现；图形化训练家、难度、队伍选择及重赛入口尚待客户端开发。现有本地回放日志已验证信息隔离，尚未与外部回放服务联调。
- 搜索本身仍有第三阶段列出的复杂状态和隐藏计数器建模边界。本阶段完整对局采用低预算或零预算功能配置，不能用作策略强度、五秒满载表现或开放验收的证据。
- 正式训练家内容、复杂状态补齐、客户端入口及每组至少 1000 局的强度验收完成前，保持正式功能关闭。

## 第五阶段：正常客户端入口与本机网页联调

### 已实现

- 在正常客户端首页加入「AI 挑战」，同页选择训练家、普通 / 高难和自己的队伍。展示公开头像、简介、风格和 FC 赛制，直接解释高难的信息权限；复用既有队伍编辑器、队伍图标、登录流程及战斗按钮，没有另建演示客户端或默认替玩家提供队伍。
- 训练家摘要仅新增经过校验的头像标识与纯文本简介。服务端保留固定队伍与高难初始快照，客户端按现有方式转义展示字段。
- 查询接口增加 `protocolVersion: 1`、当前玩家标识、断线保留时长及可选的挑战结果。三参数挑战使用请求标识关联 `pending / success / error`，相同标识不重复创建；客户端超时仅查询结果，忽略不相关或旧身份的响应。原两参数开发命令仍可使用，原天梯验证默认弹窗行为不变。
- 挑战失败能收到实际队伍校验原因并恢复操作。页面分别处理关闭、无训练家、读取失败、无队伍、选择失效、创建中、已有对局、名额满和断线；不将读取失败误报为无训练家。
- 选择记忆按服务器与玩家身份保存。所选队伍通过稳定标识关联，编辑及排序不会错选；重新加载仅恢复唯一的精确匹配，队伍删除或重复歧义会要求重选。提交前刷新编辑器保存的队伍内容。未命名玩家完成原有身份步骤后保留当前选择。
- 原战斗界面接收仅发给对局玩家的 AI 元数据，重连仍能识别训练家和难度；公共房间日志与回放不包含该私有元数据。AI 结束后的「重新挑战」回到选择页并恢复该局设置，不向虚拟账号发起真人挑战，也不自动开新局；保留既有回放功能。
- 加入显式本机启动命令。服务端只在本地模式启用开发样例并强制监听回环地址；客户端托管正常构建，配置可用的本地资源和 WebSocket 地址，通过真实官方身份服务完成原有认证。未改写实际 `config/config.js`、正式训练家内容或线上配置。

### 2026-09-07 验证结果

- 两个仓库构建成功，改动的服务端与客户端 JavaScript / TypeScript 通过定向 ESLint，两个仓库的 `git diff --check` 通过。
- 服务端执行上述本机说明中的回归命令，共 **171 项通过**：原有 154 项、7 项新增 AI 协议与公开摘要测试、10 项现有天梯验证回归。新增覆盖请求幂等与待处理查询、用户隔离、实际队伍校验错误、容量与不可用错误、头像 / 简介校验和私有重连元数据隔离。
- 客户端新增 **11 项通过**：导航、默认难度与身份隔离、队伍编辑 / 排序 / 删除 / 重复歧义、避免导入副本抢占原队伍标识、防重复提交与超时查询、不确定结果恢复、重赛不自动创建、状态提示与 HTML 转义、协议版本和损坏的本地请求记录；另验证登录恢复不会被访客旧默认覆盖，同时保留本次登录前主动修改的选择。
- 正常网页 `http://localhost:8080/` 使用原有身份界面和测试昵称，通过既有编辑器创建并导入六只队伍。实际验证首次普通、缺少队伍、单只队伍被拒且显示六只限制、编辑后重新提交当前队伍、创建中与已有对局状态、选择保留及高难说明。
- 实际普通档房间 `battle-gen9fcag-7814` 完成队伍预览和三个回合：首发、鬼火与烧伤结算、太晶化和招式、换人及血量更新均由真实引擎执行。AI 共决策 5 次、推演 768 次，超时、工作线程错误、搜索回退和非法行动均为零；随后通过界面认输。单局仅用于功能验证，不构成强度或满载性能证据。
- 刷新后通过正常身份流程恢复同一昵称，回到同一房间及原回合请求；验证了断线提示、认输、下载回放入口和重赛返回选择页且不自动开战。最后一次网页复查另验证了保存高难与所选队伍后，刷新并正常登录能恢复两项选择。桌面和 `390 × 844` 手机尺寸下检查了正常入口、选择页与实战操作布局。

已有全仓检查限制仍存在：服务端 TypeScript 安装版本与配置不符，指定 `--ignoreDeprecations 5.0` 后仍是前阶段记录的六处既有错误；客户端同样有 TypeScript 版本不符，覆盖该参数后报告既有 `play.pokemonshowdown.com/src/panel-teamdropdown.tsx:1162` 的 `FormatData.collapsed` 类型错误。客户端原有完整 Mocha 入口还会在 `battle-dex.js` 引用浏览器 `document` 时中断；本阶段隔离的 AI 测试不受影响。不能表述为两个仓库的完整测试或类型检查已通过。

### 第五阶段当前边界

- 网页高难实战及完整异常矩阵尚未验收完成。高难开战动作被自动审批拦截，理由是认为第二场真实高难对局超出本次开发授权；已向用户请求继续本机网页验收。未在获准前改用协议或脚本绕过该拦截。容量占满、关闭 / 无训练家、失效选择等已有逻辑测试，仍需按设计补完相应实际浏览器流程。
- 回放下载入口已在本机网页验证，外部回放上传服务未提交验收数据。
- 正式训练家内容、第三阶段列出的复杂状态还原、每组至少 1000 局的策略强度和满载性能验收仍未完成。正式功能保持关闭。

## 2026-09-07 扩展：对战前选择赛制与编辑 AI 队伍

- 按用户新要求增加 FC UBUU、FC OU、FC UU 三个赛制选项。FC UBUU 对应引擎现有 `gen9fcubersuu`，另两个 ID 为 `gen9fcou` 和 `gen9fcuu`，没有新增或改写这些赛制规则。
- 玩家先选赛制，再选择该赛制的训练家；切换赛制清除不匹配的训练家，保留玩家队伍并在提交时重新校验。赛制纳入按服务器与身份保存的选择，以及对局私有元数据、断线恢复与重赛选择。
- 查询协议升级为 `protocolVersion: 2` 并返回三个赛制摘要。网页挑战明确提交赛制，服务端拒绝不在选项中的赛制及训练家与赛制不匹配的请求。原有开发命令保留兼容；重赛命令也核对原对局赛制。
- `config/fantasy-ai-trainers.example.json` 提供三个合法的本机开发样例，其中 FC UU 使用符合该赛制的独立队伍。旧 AG 策略基准移至 `test/fixtures/fantasy-ai-trainers.json`，原有测试与离线联调继续使用该固定基准。
- AI 队伍仍通过 `config/fantasy-ai-trainers.ts` 中每个配置项的 `format` 和 `team` 编辑；一个配置项对应一套赛制队伍。提供完整中文编辑说明、六只队伍代码模板和 `npm run validate:fantasy-ai` / `npm run validate:fantasy-ai:examples` 校验命令。本机模式优先使用该配置文件，仅在数组为空时加载开发样例。
- 用户要求停止测试前，已完成服务端 **176 项**、客户端 **14 项**定向回归、三个开发样例合法性校验、构建与改动范围 ESLint。新增服务端回归为三个赛制各自创建真实房间，完成预览及首回合，并验证赛制不匹配和玩家队伍违法时拒绝创建。类型检查仍只报告此前六处既有错误。
- 用户明确要求自行测试后，停止网页游玩验收；尚未将新的赛制选择流程记录为完整浏览器对战验收通过。
- 8000 端口另有正常服务运行，本机 AI 服务现使用 `127.0.0.1:8001`，正常客户端仍为 `http://localhost:8080/fantasyai`。两边通过 `FANTASY_AI_LOCAL_PORT` 指定同一端口。直接打开 `testclient.html?~~localhost:8000` 会继续连接 8000 服务；其 AI 开关关闭时，输入昵称也仍会显示尚未开放。

## 2026-09-07 最新调整：首页顺序与统一 8000 服务

- 按用户要求，将首页「AI 挑战」移到「队伍编辑器」正下方。
- 实际 `config/config.js` 已设置 `fantasyai.enabled: true` 和 `allowDevelopmentTrainers: true`，原有默认 8000 服务常规启动即可提供 AI 挑战，无需额外启动参数。正式训练家数组非空时优先使用正式配置；为空且明确允许开发训练家时，加载三个带开发标记的样例。
- 客户端恢复固定连接 8000，移除此前用于 8001 联调的端口环境变量。已关闭额外的 8001 AI 服务，使用普通启动方式重新启动 8000 服务与 8080 客户端。
- 同步更新本机启动和队伍编辑说明。本次只完成构建与服务启动检查，按用户要求未运行测试或进行网页对战操作；此前测试记录不代表这些最新改动经过复测。

## 2026-09-07 扩展：撒钉与清钉时机

- 新增 `server/fantasy-ai/hazards.ts`，为规则评分和整回合推演提供共同的钉子估值，并添加中文多行开发注释。根据存活队伍、入场伤害、层数、厚底靴、漂浮及 Fantasy 的战斗盔甲 / 健壮胸肌免疫、近期换人和轮转招式估算收益；对方最后一只已在场时不再奖励撒钉。
- 对即将被钉死或入场后濒死的候补增加清钉收益，配置的 `keyMembers` 同时影响规则与推演。换入承伤按先扣除钉子伤害后的血量估算；毒系成员安全入场吸收毒菱也能作为清钉选择。
- 毒菱计入层数边际收益、毒系吸收、已有异常、免疫和毒疗等情况；黏黏网按速度关系、能力下降免疫、唱反调与不服输等反向获益估算。
- 单招原生执行记录双方钉子的实际变化，覆盖追加层数、清除、反射、换场和攻击附带的钉子。除雾、大扫除会同时扣除清掉对方场上己方钉子的损失；清钉被免疫、撒钉达到上限或无后续入场收益时不会得到虚假奖励。
- 规则评分继续比较当前威胁、生存与击杀机会；己方六个候选和对手四个候选均为有净收益的清钉、撒钉行动保留位置，仍遵守原有候选上限和预算。同步更新训练家配置注释与队伍编辑文档。
- 已完成服务端构建与上述五个策略模块的定向 ESLint 检查；全仓类型检查使用 `--ignoreDeprecations 5.0`，仅报告前文记录的六处既有错误，没有本次改动的类型错误。本次按用户要求不运行测试或网页对战，启动 / 重启默认 8000 服务后加载新逻辑。
- 钉子长期收益仍为有限视野启发式，普通档的未知配置由公开信息推测；自定义入场计数器、复杂状态与策略强度仍需后续验证。

## 2026-09-07 扩展：全 Fantasy 训练家

- 按用户要求在 `config/fantasy-ai-trainers.ts` 加入「幻想先锋·岚」（`fantasy-vanguard-ou`），采用 FC OU、均衡风格，六只宝可梦与六种道具全部为 Fantasy 内容，共携带九个适配的 Fantasy 招式。
- 盔甲鸟负责碎菱钢与隐形岩；帝王拿波带除雾和变速折返；快龙使用特攻配装；武道熊师使用 FC OU 允许的连击流 2（格斗／超能）；罗丝雷朵通过技术高手、玫瑰之舞、晶光转转与花之舞进攻和清场；鸭嘴炎兽以超级发射器和幻想标靶搭配四攻。
- 配置旁附逐成员多行注释，解释道具与特性、招式的搭配，关键成员设置为第 3、4、5 只。同步更新训练家编辑、启动和设计文档。正式配置非空后，原三个开发样例不再自动回退显示。
- 服务端构建与 `tools/validate-fantasy-ai-trainers.js` 配置合法性校验均已通过，确认完整六只队伍可用于当前 FC OU。按用户要求未运行测试或网页对战；实际策略表现由用户自行确认。

## 2026-09-08 扩展：根据用户回放加强策略

- 已静态阅读用户提供的 FC OU 回放：第 12～15 回合重复输出被偷懒补回，第 22～25 回合快龙重复羽栖并放任闪焰高歌强化，第 27～28 回合盔甲鸟在己方只剩两只时仍继续铺钉。回放不含内部候选分数，不能据此确定每一步的内部决策原因。
- 增加公开行动、伤害、回复和回合末血量记忆。未发生公开属性 / 异常 / 能力等级 / 道具等变化的非暴击伤害用于修正单招估计；记录无进展输出和连续回复，结合速度证据判断行动先后。
- 对手配置扩展为进攻、物耐、特耐等假设，保留已公开招式；根据回复 / 干扰招式调整配置权重。对手回应按局面收益和已公开用招频率分配概率，主动换人仍面对基于当前在场成员预测的回应。
- 规则评分加入可持续回复判断、回复 PP 上界、重复输出 / 回血的机会成本和对手强化威胁；特殊机制候选按变化后的防守资料估算承伤。换入评估包括钉子、入场攻击、后续行动机会、安全盾牌的回复 / 铺钉价值；残局钉子收益按剩余血量、存活人数与承伤风险缩减。
- 统一默认昵称与 20 字截断，避免假想世界使用错误的队员名称；修正对手配置变体在世界构造中的使用。补充盐腌、同命、已公开幻防宝石持久效果的还原，不以静默删除未知复杂状态换取搜索成功。
- 搜索从一回合比较逐步加深到最多三回合；根部仍最多六候选，深层保留三个，后续双方重新决策。普通 / 高难共用五秒上限与最多 192 个模拟回合的工作量预算；不完整的深层比较不会覆盖已发布结果。
- 结束统计增加 `searchDepths` 与 `fallbackReasons`，用于区分未完成搜索和完成搜索后决策仍不佳。只记录统计及原因代码，保持公开回放和私有配置的边界。
- 新增 [AI-FANTASY-STRATEGY.md](AI-FANTASY-STRATEGY.md)，列明回放问题、代码入口、搜索上限、观察统计和当前建模边界；同步更新设计与本机启动文档。训练家队伍不变，仅将幻防宝石注释同步为主分支已更新的首次招式伤害触发规则。

本轮检查：`node build` 成功；11 个改动策略 / 控制模块的定向 ESLint 通过；`git diff --check` 通过。全仓 TypeScript 检查使用 `--ignoreDeprecations 5.0 --incremental false`，仍只有前文记录的六处既有错误，没有本次改动的新类型错误。

按用户要求，**未运行自动化测试、离线策略模拟或网页对战**。构建与静态检查不证明策略强度，实际搜索深度、决策耗时及回放行为由用户重启原 8000 服务后继续确认；本轮未重启或另开后台服务。

## 2026-09-08 扩展：无效行动换人与 Fantasy 机制适配

用户要求先写开发文档再开发、不确定的机制询问用户；沿用用户自行测试的要求。先创建 [AI-FANTASY-COMPATIBILITY.md](AI-FANTASY-COMPATIBILITY.md)，随后完成本轮实现。本轮涉及的规则均能从本地引擎确认，没有修改战斗机制或引入双打范围。

- 原生探针识别无有效收益的行动，在规则结果和搜索候选发布前过滤。所有留场行动均无效且有合法换人时比较替补；被困/最后一只仍保留合法兜底。增加 PP 为零的枚举检查，特殊机制单独评估。只提高能力但依旧无法突破免疫的候选也进行过滤。
- 吸收攻击给对方引火等强化不再当作我方收益；真正成功的折返才获得轮转收益。双宝可梦探针使用公开/假设的替补数量，避免因缺少替补实体误判瞬间移动。工作进程无结果时的应急选择只使用公开证据和静态免疫判断。
- 新增共享机制解释和状态恢复模块，修正幻想冰之石、幻想生命宝珠、电疗、美梦共游的行动例外，以及炙疗、电疗、增幅系统、冻伤等回复/持续伤害估值。
- 鬼影已触发标记、属性宝石永久增伤、幻防宝石持久效果及暗影之瓶公开扣血区间接入单招与整局重建；幻想究极能量增益和异兽提升暂时失效状态接入白名单。消耗后提示不再把道具重新记作持有。
- 恢复许愿和优质培育的延迟位置回复，跨换人保存原使用者；区分延迟治疗与当前回合救命。加入按招式与角色排序的 Fantasy 道具候选，并保留赛制过滤、普通道具候选和已知配置优先级。
- 修改形态仍使用 Fantasy Dex 和合法初始配置；当前公开状态独立还原。寄生的目标为相邻队友，当前单打没有合法目标，已从单打配招先验排除。

检查记录：`node build` 成功；15 个本轮模块的定向 ESLint 和 `git diff --check` 通过。全仓类型检查仍只有前文记录的六处既有错误。**未运行测试、模拟对战或网页对战，未重启/另开服务。** 详细人工验收场景和剩余假设边界见本轮开发文档。

## 2026-09-08 扩展：训练家具备四类机制

- 先在 [AI-FANTASY-TEAMS.md](AI-FANTASY-TEAMS.md) 编写开发约定。用户确认保留现有规则：Mega、Z 招式、太晶化、气场爆发四类都可选，Z 与气场按局面择一，共用全队一次机会。
- 「幻想先锋·岚」的 `resourcePreferences` 配齐四项。盔甲鸟改带 `Skarmorite`，继续承担碎菱钢／隐形岩撒钉；第五只由罗丝雷朵换为幻想兰螳花，携带 `Grassium Z`，使用叶刃、穿云、花之舞与急速折返，提供 Z 招式与气场候选。
- 其余四只保留原配装与太晶资格，帝王拿波继续清钉。队伍仍全员 Fantasy 宝可梦，共八个 Fantasy 招式；道具调整为四件 Fantasy 道具、Mega 石与增加了气场功能的草 Z。更新配置中的多行注释、成员表与设计说明。
- 原生气场资格可能在使用 Z 后仍保留开局缓存；AI 候选枚举增加公开资源消耗约束，规则策略与工作进程应急回退均接入，假想世界按相同规则关闭已消耗的气场资格。保留原版奈克洛兹玛究极爆发与 Fantasy 气场的区分，未修改玩家赛制或战斗规则。
- 不强制每局使用机制；AI 按伤害、生存、形态变化及资源代价比较普通行动与机制候选。

检查记录：`node build`、五个改动文件的定向 ESLint（`--max-warnings 0`）、`node tools/validate-fantasy-ai-trainers.js` 和 `git diff --check` 通过，完整六只队伍符合当前 FC OU。全仓 TypeScript 检查仍为此前记录的六处既有错误，没有本轮新增错误。**按用户要求未运行测试、策略模拟或网页对战，未重启服务。** 重启原 8000 服务并重新创建 AI 挑战后加载更新。

## 2026-09-08 扩展：官服高分回放研究与连续换人修正

- 先静态阅读用户第二份回放，写 [连续换人与异常修正开发文档](AI-FANTASY-SWITCHING.md)。用户要求先学习官服顶分 6v6 后，在改策略代码前读取官方评分排序接口及 10 份公开完整回放，共 367 个回合标记；OU 标注分 2048～2116，National Dex 1959～1992。逐回合证据、来源和从成功／失败轮转得到的修订见 [研究记录](AI-FANTASY-HIGH-LADDER-STUDY.md)。
- 修复候补残留能力等级、盐腌及临时属性。公开换人记录区分主动、折返、强制与初始上场；循环惩罚只针对同一对手面前的净损失与无进展，并传入搜索。保留无损轮转、必要补位和危险牺牲候选。
- 换入先计承伤，再折扣后续攻击／撒钉机会；通过公开行动顺序识别先手折返后尚未结算的对方行动。移除把普通换人视作消除已有灼伤／中毒的虚假奖励，保留有实际机制支持的自然回复、再生力与清除盐腌收益。
- 异常成本区分成员职责和 Fantasy 例外；鬼火等造成的状态、降能力及持续干扰接入留场和换人风险。原生资料支持本回合先手异常后的攻击修正，以及异常增伤、闪焰高歌等附带强化的后续压力。搜索终局同步角色异常成本。
- 加入有生存窗口与实际攻击 PP 支持的有限回复 PP 压制，避免“伤害不过半就换人”。新增分析保持有界，没有扩大五秒预算或搜索深度上限，没有更改四机制规则或训练家配置。

最终检查：`node build` 成功；7 个本轮改动模块的定向 ESLint（`--max-warnings 0`）与 `git diff --check` 通过。全仓 TypeScript（`--noEmit --ignoreDeprecations 5.0 --incremental false`）仍为此前记录的 6 处旧错误，位置为 `data/mods/gen8/rulesets.ts:98,126`、`data/mods/gen9ssb/scripts.ts:963`、`data/random-battles/gen7/teams.ts:1607`、`server/chat-plugins/othermetas.ts:130,131`，没有本轮新增错误。

按用户要求，**未运行测试、策略模拟或网页对战，未重启或另开服务**。策略效果、实际推演深度与耗时尚需实战确认；重启原 8000 服务后新建挑战加载更新。

## 2026-09-08 扩展：取消 AI 思考时间限制

用户明确取消思考时间限制，本节替代前面历史记录的五秒／192 回合／三回合约定。先编写 [AI-FANTASY-THINKING.md](AI-FANTASY-THINKING.md)，再修改实现。

- 本机实际 `config/config.js`、示例配置与共享默认值改为 `decisionMs: null`，并贯穿管理器、控制器、调度器和工作线程消息。不安装不限时任务的截止定时器；取消、新请求替代、房间结束、线程异常的生命周期处理仍保留。显式非负毫秒预算依然支持，`0` 仍是立即兜底，原五秒夹限已移除。
- 跨线程用 JSON 的 `null` 表达不限时，内部计算才使用无限截止点。显式极长预算分段设置 Node 定时器，避免超过 32 位毫秒范围后被立即超时。
- 默认取消 192 个模拟回合的工作量截断。搜索计划加深至最多六回合，根部八候选、对手四候选、四世界、两样本；不限预算时每层完整比较可支持的场景与样本，并保留根部候选进入深层。显式有限预算仍使用渐进发布和深层四候选。
- 核对已有 `RoomBattleTimer.start()`：AI 挑战本来就禁止回合倒计时，本轮沿用。创建不限时挑战时增加等待提示，不改变信息权限、四机制规则或训练家配队。未提高工作线程数、并发或内存上限。
- 更新当前策略、设计、本机配置与上一轮修正文档。首发和强制换人仍走现有规则入口，六回合只表示正常出招请求的有限搜索计划，不代表完整博弈解或已验证强度。

静态检查：`node build`、7 个本轮文件的定向 ESLint（`--max-warnings 0`）、本机实际配置的 `node --check config/config.js` 和 `git diff --check` 通过。全仓 TypeScript 仍仅有上节记录的 6 处旧错误，没有本轮新增错误。

按用户要求不运行测试或对战模拟；本轮未重启服务，重启原 8000 后新建挑战加载更新。不限时可能增加每回合等待，实际耗时与策略表现由用户测试。

## 2026-09-08 扩展：常规十秒与少量关键二十秒

用户实战反馈不限时等待过长，要求普通思考十秒内、极少量关键回合允许二十秒。本节替代上一节不限时默认值；先写 [AI-FANTASY-TIME-BUDGET.md](AI-FANTASY-TIME-BUDGET.md) 再实现。

- 本机实际配置、示例与共享默认改为 `decisionMs: 10000`、`criticalDecisionMs: 20000`。管理器、控制器、调度器和工作线程贯通普通／关键标记；二十秒为收到请求起的总上限，不是追加时长。排队与线程启动包含在内。
- 新增 `time-management.ts`：从第十回合起，每局最多两次关键额度，至少间隔十回合。只根据己方请求与公开日志中的残局资源、低血量收割或强化威胁分配；首发、强制换人和单选请求不自动加时。额度在分配时消耗，提前完成不退还，原因只进入开发统计。
- 同一回合、阶段与在场成员的重试共享原截止点，新的请求编号或招式不可用提示不能重新领取十／二十秒。补位是独立普通决策窗口。增加 `criticalDecisions` 与 `criticalDecisionReasons` 统计。
- 常规最多六根候选／三深层候选／四回合，关键最多八／四／六；渐进发布公平比较，按已完成轮次的耗时预估新一轮成本，并预留最多 150 ms 给返回消息。主线程定时器仍是最后截止保障；没有工作线程结果时使用控制器已准备的静态合法兜底，不退回任意第一个候选。
- 更新当前策略、设计、本机使用文档并标明旧不限时文档属于历史阶段。沿用认输取消、AI 对局不设玩家回合倒计时、普通／高难信息权限及四机制规则。

静态检查：`node build`、9 个本轮文件的定向 ESLint（`--max-warnings 0`）、本机实际配置的 `node --check config/config.js` 和 `git diff --check` 通过。全仓 TypeScript 仍只有前文记录的 6 处既有错误，没有本轮新增错误。

按用户要求没有运行测试、离线对战模拟或网页对战，也没有重启服务。重启原 8000 服务并新建挑战后生效；实际耗时、关键识别及策略表现留给用户验收。

## 2026-09-09 阿塞萝拉 UBUU / UU 队伍与粗骨头识别

按 [AI-FANTASY-TEAMS.md](AI-FANTASY-TEAMS.md) 本轮开发约定完成：

- 正式配置新增 `acelora-ubuu`（FC UBUU）和 `acelora-uu`（FC UU），名称和头像与现有 FC OU 阿塞萝拉一致。两档难度共用对应赛制的固定队伍，原有 OU 配装保留。
- 十二名成员按用户提供的文本录入；UU 嘎啦嘎啦采用后续补充的 `248 HP / 252 Atk / 8 SpD`、固执性格，没有添加零速度个体值或另选太晶属性。
- 客户端 `battle-dex-search.ts` 的粗骨头推荐按 Cubone / Marowak 基础物种识别 Fantasy 形态。服务端原有攻击翻倍效果已支持该形态，无须改动战斗机制。

验证结果：

- 两端构建通过；服务端首次构建遇到已有 `dist` 产物的写入权限限制，经授权重试成功。客户端缺少 PHP 的新闻生成提示不影响构建完成。
- 三支正式队伍均通过 `npm run validate:fantasy-ai`，注册、压缩 / 解压及对应赛制的玩家校验也通过。
- 加载实际构建后的客户端数据与搜索模块，确认粗骨头在 Fantasy 阿罗拉嘎啦嘎啦、原版嘎啦嘎啦、原版阿罗拉嘎啦嘎啦和卡拉卡拉的专属道具分组各出现一次；不会误归入谜拟丘专属道具，伽勒尔呆壳兽的 Mega 石限定也未放宽。
- 使用 Fantasy 引擎创建局面，确认用户配装的嘎啦嘎啦攻击为 306，携带粗骨头后为 612；此检查没有推进对战回合。
- 客户端 `test/fantasy-ai.test.js` 的 14 项回归检查通过。训练家配置 ESLint 通过；客户端搜索文件与 HEAD 基线相比，仍为既有 116 个错误、3 个警告，没有新增诊断。
- 未重启现有 8000 服务，未运行完整网页挑战或多回合 AI 对战。重启服务端并刷新客户端后加载本轮队伍与道具分组修复。

## 后续阶段

1. **复杂状态一致性**：补齐槽位效果、变身、链接状态、自定义特性计数器与自身候补 PP，扩大跨 FC 赛制及特殊形态的真实引擎对照测试。
2. **客户端验收**：第五阶段入口已实现，继续补完实际浏览器异常矩阵与高难游玩验证。
3. **策略与开放验收**：补充训练家及其他赛制内容，准备版本化且不参与调参的评测集，每组至少 1000 局、位置与控制策略交换、置信区间及降级比例；普通对贪心至少 75%、高难对普通至少 60%，并进行满载测试。

本机默认 8000 已按用户要求开启 AI，正式配置为阿塞萝拉的 FC UBUU、FC OU、FC UU 三支队伍。其他训练家内容、完整网页验收、复杂状态和强度门槛仍待补充或验证。
